# 设计母线导航：Prompt 编译链、治理收费链与源码质量证据梯度

这一篇不新增新的机制判断，而是把 Claude Code 最近已经稳定下来的三条高阶判断压成可直接进入的阅读入口。

它主要回答三个问题：

1. 如果想理解 prompt 的效力来源，现在最稳的阅读入口是什么。
2. 如果想理解安全设计和省 token 设计为什么共享同一条治理收费链，应该从哪里进入。
3. 如果想真正学 Claude Code 的源码先进性，应该学结构本身，还是学它的演化方法。

## 0. 先问你是不是在抄错东西

在进入这三条判断前，先停一下，问自己准备模仿的到底是什么：

1. 如果你想抄的是一段更强的 system prompt，你已经把 Prompt 效力读浅了。
2. 如果你想抄的是更多 modal、allowlist 或 compact 技巧，你已经把安全与省 token 读浅了。
3. 如果你想抄的是更整齐的目录树或更多小文件，你已经把源码先进性读浅了。
4. 如果你想优化目录结构的方式只是再补一个 README 或更长的 route map，你已经把目录优化读浅了。
5. 如果你把 lawful forgetting 读成“更会总结”，把 classifier 读成“额外保险”，或把 `remote / session / transport` 读成“并列功能层”，你已经把三条母线都抄成了投影层。

这页真正要纠正的不是阅读顺序，而是模仿对象本身：Claude Code 最难学会的从来不是表面做法，而是三条判断各自保护什么不变量。
`05` 因而也只负责校正“别抄错东西”，不重发 Prompt / governance / source-quality owner 页已经拥有的首答公式、witness order 或 canonical ladder。

这里还应再固定一条 continuity 纪律：

- `continuity` 不是第四条母线，而是 Prompt `Continuation`、治理 `continuation pricing` 与源码质量 `recovery non-sovereignty / anti-zombie` 在时间轴上的共同交汇。
- 如果你现在还把 `/compact / /resume / /memory` 当成第四类设计对象，你抄到的就仍是按钮表，不是三条母线怎样处理“继续”。

## 0.2 第一刀先拒什么

如果想在进入长链前先做一次更狠的模仿对象校正，也只先拒三件事：

1. Prompt
   - 一旦 lawful forgetting 会改写 `reject / continue verdict`，或者 `card / verdict / prose` 越位成主语，你抄到的就不是 compiler，而是 narration。
2. 治理
   - 一旦 modal、token 面板或 classifier 被当成免费裁判，你抄到的就不是 pricing chain，而是治理 UI。
3. 源码质量
   - 一旦目录树观感、feature matrix 或 remote 模块分组先于 `current-truth surface / truth planes / later-maintainer reject path` 出现，你抄到的就不是结构治理，而是结构体感。

这三刀如果先拒不掉，后面的阅读顺序再对，也只是在更深的位置继续抄错东西。

如果还想先做一刀 continuity 去魅，也只先拒三件事：

1. 把 summary prose 当 continuity 主语。
2. 把 continuation pricing 当“默认免费续聊”的 UX 优化。
3. 把 recovery asset / replay transcript 当成 current truth signer。

## 0.1 并行深挖时不要按文件拆题

如果要并行多 Agent 深挖这三条判断，更稳的拆法也不是：

- 甲改前半本，乙改后半本
- 甲看 Prompt 文件，乙看治理文件，丙看结构文件名

而是先按四类第一问题拆：

1. Prompt 世界准入
   - `same-world compiler` 到底由谁定义。
2. 治理定价
   - 哪些扩张被谁收费、谁有资格继续。
3. present-truth 保护
   - 谁配宣布现在、谁只能当恢复资产或消费者投影。
4. 目录前门纪律
   - 这些问题在蓝皮书里第一次该由哪一层回答。

合并时也只该回到同一组 merge frontdoor：

- `09`
  定义控制面问题。
- `05`
  校正模仿对象。
- `15`
  暴露第一条反证信号。
- `41`
  把高阶结论压回第一性原理。

如果并行之后得到的是四份更长的对象清单，而不是四类问题在这组 frontdoor 上重新合流，就说明拆题方式本身已经让目录重新长回第二套主语。

更深一层说，真正成熟的目录前门还不只要在 `09 / 05 / 15 / 41` 上合流主语，也要合流顺序：

1. Prompt 线最终要回同一条 witness order。
2. 治理线最终要回同一条 failure order。
3. 当前真相线最终要回同一条 reject order。

如果并行之后每个分支都还在各自发明“先看什么”的顺序，入口层即使共用了主语，也还没有真正完成合流。

如果继续把 continuity 的最小顺序再压成一行，它也只该剩：

- `continue qualification -> continuation pricing -> recovery non-sovereignty / anti-zombie`
- 这条顺序只配作为三条母线的时间轴，不配单独升级成第四套目录主语。

## 1. Prompt 编译链投影

适合在这些问题下阅读：

- 为什么 Claude Code 的 prompt 不是一段更好的文案。
- 为什么它更像上下文准入编译器、协议转译器和 protocol surface。
- 为什么 protocol transcript、tool schema、delta attachments 都该放在同一条线上。

稳定阅读顺序：

1. `07-运行时契约、知识层与生态边界.md`
2. `architecture/28-提示词契约分层、知识注入与缓存稳定性.md`
3. `architecture/39-Prompt可重放前缀、可观测预算与Section编译器.md`
4. `architecture/53-稳定前缀、动态尾部与旁路Fork：Claude Code的Cache-Aware Prompt Assembly.md`
5. `architecture/54-从UI Transcript到Protocol Transcript：Prompt不是聊天记录的直接重放.md`
6. `architecture/61-工作语法机：Claude Code如何把软件工程世界编译成模型可行动的Protocol Surface.md`
7. `philosophy/18-Prompt不是文本技巧而是契约分层.md`
8. `philosophy/39-治理必须落到字节级确定性：上下文准入优于功能堆叠.md`
9. `philosophy/48-先把世界压缩成可治理语法，再让模型变聪明.md`
10. `architecture/64-语义压缩器：Claude Code如何把工作现场压成可继续行动的最小语义体.md`
11. `philosophy/51-真正强的Prompt不是信息更多，而是行动语义更密.md`
12. `architecture/67-协调成本控制面：Prompt如何同时组织模型推理与人类接手.md`
13. `philosophy/54-好的Prompt同时组织模型与人类接手路径.md`
14. [Transcript、Lineage与Continuation如何组成协作接口](<../philosophy/57-Transcript、Lineage与Continuation如何组成协作接口.md>)

这条线的核心结论是：

- `05` 在 Prompt 线上只校正模仿对象：别抄更长文案，去抄 `same-world compiler` 保护的制度对象。
- Prompt 顶层定义、`first-reject path` 与公开 owner route 继续只回 `philosophy/84`；builder-facing implement / audit 只配留在 owner chain 内部展开。
- 这页若还开始重写 Prompt 公式、canonical witness order 或 object list，就会从“校正模仿对象”退回“第二 Prompt 前门”。

继续往下时，这页只保留两种 next hop：

1. 缺 Prompt 顶层定义、完整公式、same-world witness 或 `continue qualification`
   - 回 `philosophy/84`
2. 缺“这页还该不该继续说、现在是不是只该 route trim”
   - 回 `06`

如果 route 页开始继续公开发放 builder-facing 二跳，或展开 `world-defining winner`、`transcript conflation`、`continuation_story_only` 与 handoff object list，它就又在偷 owner。

## 2. 治理收费链投影

适合在这些问题下阅读：

- 为什么扩张定价会同时投影成安全设计与省 token 设计。
- 为什么 Claude Code 偏爱先裁剪、后暴露、再外置。
- 为什么 deferred、delta、tool result externalization、continuation pricing 本质上同构。

稳定阅读顺序：

1. `architecture/21-消息塑形、输出外置与Token经济.md`
2. `architecture/37-统一治理收费链：能力裁剪、continuation pricing 与状态外化.md`
3. `architecture/50-PolicySettings主权链、Sandbox契约与治理收费顺序.md`
4. `architecture/51-安全即输入边界控制平面：Managed Authority、Trusted Sources与Runtime Boundary Compilation.md`
5. `architecture/56-外部真相链如何延后承认能力：Deferred、Delta与最小进入面.md`
6. `architecture/62-Narrow、Later、Outside：安全设计与省Token设计的统一反扩张运行时.md`
7. `philosophy/22-扩张定价不是三项目标权衡，而是同一条治理收费链.md`
8. `philosophy/38-安全、治理、Token与Prompt稳定性本质上是同一收口问题.md`
9. `philosophy/49-真正的省Token不是压缩文本，而是改变世界暴露给模型的方式.md`
10. `architecture/65-资源宪法：Claude Code如何统一分配能力、时间、注意力与权威.md`
11. `philosophy/52-模型不是资源的主人，Runtime才是.md`
12. `architecture/68-有效自由控制面：为什么约束不是在削弱Claude Code的行动力.md`
13. `philosophy/55-真正的自由不是少约束，而是约束不再破坏行动.md`
14. `philosophy/58-真正的安全不是拦截动作，而是给行动、能力与时间统一定价.md`

这条线的核心结论是：

- `05` 在治理线上只校正模仿对象：别抄 modal、allowlist、compact 技巧，去抄“拒绝免费扩张”的 pricing chain。
- 治理 canonical formula 继续回 `10`，why 继续回 `85`，runtime seam 继续回 `83`，question ladder 继续回 `100`；这页不再代签它们的第一句。
- 如果你缺的是弱读回面、用户侧 readback 或机制翻译，继续跳 `10 / security / risk`，不要在这里再造治理短卡。

继续往下追时，最容易写浅的还有两件事：

1. 把 `typed ask` 写成一个 modal，而不是围绕 `request_id / tool_use_id` 的分布式仲裁事务。
2. 把 `Context Usage` 写成成本仪表，而不是 `decision window` 是否仍有决策增益的证据面。

如果继续把治理线再压成一条入口版总括，也只该再补一句：

- 真正值钱的不是审批 UI，而是 `verdict seams`；谁能出 verdict、谁能接管、谁能撤销、谁负责 rollback，这条缝写清后，`typed ask` 才从 modal 升级为治理事务。

如果继续从“秩序中的高行动力”角度读：

1. `architecture/23-统一权限决策流水线与多路仲裁.md`
2. `architecture/51-安全即输入边界控制平面：Managed Authority、Trusted Sources与Runtime Boundary Compilation.md`
3. `architecture/62-Narrow、Later、Outside：安全设计与省Token设计的统一反扩张运行时.md`
4. `architecture/65-资源宪法：Claude Code如何统一分配能力、时间、注意力与权威.md`
5. `architecture/68-有效自由控制面：为什么约束不是在削弱Claude Code的行动力.md`
6. `philosophy/52-模型不是资源的主人，Runtime才是.md`
7. `philosophy/55-真正的自由不是少约束，而是约束不再破坏行动.md`
8. `philosophy/58-真正的安全不是拦截动作，而是给行动、能力与时间统一定价.md`

## 3. 源码质量证据梯度投影

适合在这些问题下阅读：

- Claude Code 的源码先进性到底该学什么。
- 为什么它不是“模块更多”就更强，而是“增长时仍能守住不变量”。
- 为什么 config、deps、state machine、leaf module 这些局部动作比目录树更值得看。

稳定阅读顺序：

1. `architecture/41-叶子模块、扼流点与循环依赖切断法.md`
2. `architecture/52-Chokepoint、Typed Decision、Authoritative Surface、Race-Aware Runtime与Contract-First：Claude Code源码先进性五法.md`
3. `architecture/58-让依赖图说真话：Leaf Module、Anti-Cycle Seam与Single-Source File.md`
4. `architecture/63-可演化内核：Claude Code如何在持续增长中维持不变量.md`
5. `philosophy/42-成熟架构不是没有大文件，而是不把复杂性撒满全仓.md`
6. `philosophy/45-先让依赖关系诚实，再让抽象显得优雅.md`
7. `philosophy/50-先进源码不是一开始就完美分层，而是增长时仍能守住不变量.md`
8. `architecture/66-演化制度设计：Claude Code如何在增长中保留重构可能性.md`
9. `philosophy/53-好架构不是更会重构，而是始终保留重构可能性.md`
10. `architecture/69-源码即治理界面：注释、命名与显式边界如何降低误改成本.md`
11. `philosophy/56-可读性不是修辞，而是防误改、防误扩展、防失忆的系统能力.md`
12. `philosophy/59-源码先进性不在静态分层，而在未来维护者也是正式消费者.md`

这条线的核心结论是：

- `05` 在源码质量线上只校正模仿对象：别抄目录观感、模块拆分和小文件体感，去抄 `current-truth protection` 与 later-maintainer rejectability。
- 证据上限与 canonical ladder 继续回 `guides/102`；why 继续回 `philosophy/87`；对象与 kernel 继续回 `architecture/84 / 63`；这页不再代签源码质量前门。
- 如果你现在缺的是 artifact contract、verdict 或 distortion，继续去 `api / playbooks / casebooks`，不要在这页继续重讲 ladder。

继续往下追时，最容易写浅的还有三件事：

1. 把 host-facing state 写成从 UI 或事件回放里推断出来，而不是明确 current-truth writeback。
2. 把 anti-zombie 读成零散 race fix，而不是一条持续拒绝 stale writer 的 temporal honesty 不变量。
3. 把 leaf module、registry 与 adapter subset 读成拆分技巧，而不是 dependency honesty、consumer subset 与宿主支持边界的共同治理。

如果继续从“可治理可读性”角度读：

1. `architecture/41-叶子模块、扼流点与循环依赖切断法.md`
2. `architecture/58-让依赖图说真话：Leaf Module、Anti-Cycle Seam与Single-Source File.md`
3. `architecture/66-演化制度设计：Claude Code如何在增长中保留重构可能性.md`
4. `architecture/69-源码即治理界面：注释、命名与显式边界如何降低误改成本.md`
5. `philosophy/50-先进源码不是一开始就完美分层，而是增长时仍能守住不变量.md`
6. `philosophy/53-好架构不是更会重构，而是始终保留重构可能性.md`
7. `philosophy/56-可读性不是修辞，而是防误改、防误扩展、防失忆的系统能力.md`
8. `philosophy/59-源码先进性不在静态分层，而在未来维护者也是正式消费者.md`

## 4. 三条判断如何互证

这三条判断不能拆开学。

1. 只学 Prompt 编译链，不学治理收费链，Prompt 效力就会退回免费扩张下的暂时效果。
2. 只学治理收费链，不学当前真相保护，`decision window / cleanup / continuation pricing` 就会失去可信宿主。
3. 只学结构优雅，不学请求装配与治理对象，源码先进性就会退回静态截图。

如果你准备继续写蓝皮书、迁移这套方法，或者拿别的 agent 系统对照它，先用同一组自校问题反复逼自己：

1. 我现在写的是对象链，还是结果词。
2. 我有没有点名同题坏解，而不只是在讲正解。
3. 我有没有写出第一条反证信号，而不只是在写高阶总结。
4. 我有没有给出 `shadow -> qualify -> cutover -> rollback` 的渐进改写顺序。

## 5. 如果把三条判断继续写成目录结构

更稳的目录优化，不该再理解为“补更多入口”，而应理解为“让同一条母线在不同目录只承担一种责任”：

1. `philosophy/` 负责回答这三条判断为什么必须这样设计。
2. `architecture/` 负责回答这些判断落成了哪些正式对象、chokepoint 与 current-truth protection。
3. `api/` 负责回答宿主究竟承认了哪些 contract，而不是回头去猜 UI 或内部实现。
4. `guides/` 负责把判断压成苏格拉底问题梯子、模板与矩阵。
5. `playbooks/` 负责把对象链压成准入、拒收、回退与闭环 verdict。
6. `casebooks/` 负责保留伪交接、假灰度、假窗口、假 current-truth 这类同题坏解。
7. `docs/` 负责记录研究过程、持久化记忆与这轮写作为何改动，不再和正文抢主语。
8. `navigation/` 只负责跨目录下一跳与前门校准，不接管深页解释本身。

如果继续把这一层再压成“发言权限表”，也只该剩：

- `philosophy`
  判必要性。
- `architecture`
  证对象链。
- `api`
  定承认面。
- `guides / playbooks`
  出可执行判决。
- `navigation`
  只路由，不代言。
- `docs`
  只存记忆，不重判正文。

目录一旦越权代言，仓库就会长出第二叙述者，later maintainer 也就不知道该在哪一层正式提出反对。

如果 `05 / 15 / 41` 重新变成长 route map，或者各目录 README 又开始同时兼做哲学解释、执行手册与记忆仓库，目录结构就会再次失去证据梯度。

更硬一点说，发言权限表也不只是排版纪律，而是 later maintainer 的正式反对位阶；目录一旦越权代言，仓库就会长出第二叙述者，局部可反对性也会立刻失效。

## 6. 一句话总纲

如果把 Claude Code 再压成一句最硬的总纲，它更像：

- 先把工作世界写成 `Authority -> Boundary -> Transcript -> Lineage -> Continuation -> Explainability`，再用治理收费链维持“谁能改边界、什么能进入当前世界、继续何时停止”，最后用源码质量证据梯度让系统在增长中不失控
