# 导读

## 1. 研究对象

本蓝皮书研究的是仓库中的 `claude-code-source-code/`，其 `package.json` 标明源码对应 `@anthropic-ai/claude-code-source` `v2.1.88`，并注明这是面向研究的解包源码，而不是可直接生产构建的原始 monorepo。

证据:

- `claude-code-source-code/package.json:2-8`
- `claude-code-source-code/README.md:1-8`

研究方法、版本锚点、证据索引与研究日志统一回 `docs/`；本页只保留主线前言，不再承担第二套导航与研究工作台职责。

## 2. 先带着三条判断进入蓝皮书

如果只带三句判断进入整本蓝皮书，请先记住：

1. Prompt 线
   - 主权链、历史链与继续链必须对同一个世界保持诚实。
2. 治理线
   - 一切扩张必须按同一价格秩序合法进入当前世界。
3. 源码质量线
   - 复杂度中心必须合法、边界必须可证、下一次重构必须仍有路。

再各往下压半层，会更容易抓住它们为什么比“功能更多”更值钱：

1. Prompt 线真正保护的不是某段 system prompt，而是 `request object / role contract / cache boundary / handoff truth` 仍在同一个世界里继续成立。
2. 治理线真正定价的不是“要不要拦一下”，而是动作、权威、上下文与时间四种扩张怎样进入当前世界。
3. 源码质量线真正排查的不是目录好不好看，而是谁在宣布当前真相、哪些恢复资产只能帮助恢复、哪些旧写入者必须被正式拒收。

## 3. 为什么要先画结构图

Claude Code 的强，不是单点 prompt 技巧，而是很多工程机制叠加出来的:

- 启动阶段极度在意 fast path。
- 核心 agent loop 足够小，但外围 harness 很厚。
- 工具、技能、插件、MCP 不是孤立系统，而是共享一套运行时装配逻辑。
- 多 Agent、远程、上下文压缩、权限系统都不是外挂，而是主路径的一部分。

如果先不把结构图画清，后面写“使用指南”和“设计哲学”会不可避免地漂浮。

## 4. 阅读后的第一判断

Claude Code 的本质不是“会写代码的聊天框”，而是一个把以下对象统一纳入一个 runtime 的系统:

- conversation transcript
- tool registry
- permission context
- slash commands and skills
- dynamic MCP connections
- background tasks and subagents
- compaction and resume
- remote and bridge transports

如果再往上压缩一层，这套 runtime 最难被抄走的，不是功能数量，而是三条制度判断：

1. 世界怎样进入模型
2. 扩张怎样进入当前世界
3. 过去与失控复杂度怎样不得写坏当前世界

所以更稳的源码阅读顺序不是：

- 先看哪个模块看起来更复杂

而是：

1. 先看谁在宣布真相
2. 再看谁只是消费真相
3. 再看哪些恢复资产与旧写入者可能重新篡位
4. 最后才看目录体感、文件大小与功能表覆盖率

一级阅读路径看 [README](README.md)，跨目录深链看 [navigation/README.md](navigation/README.md)，研究工法与推进记忆看 [../docs/README.md](../docs/README.md)。
