# 运行时契约、知识层与生态边界

这一章回答四个问题：

1. 为什么 Claude Code 越往深处看，越不像“一个强提示词 + 一堆工具”。
2. 提示词、知识层、权限、协作、生态边界为什么必须被一起理解。
3. 这一轮继续深挖后，蓝皮书主线该怎样继续收束。
4. 从第一性原理看，什么才是最难被抄走的部分。

## 0. 代表性源码锚点

- `claude-code-source-code/src/constants/prompts.ts:444-576`
- `claude-code-source-code/src/utils/systemPrompt.ts:28-122`
- `claude-code-source-code/src/context.ts:113-188`
- `claude-code-source-code/src/utils/attachments.ts:817-913`
- `claude-code-source-code/src/utils/claudemd.ts:1-26`
- `claude-code-source-code/src/memdir/memdir.ts:187-260`
- `claude-code-source-code/src/services/SessionMemory/sessionMemory.ts:76-181`
- `claude-code-source-code/src/coordinator/coordinatorMode.ts:111-280`
- `claude-code-source-code/src/services/mcp/channelNotification.ts:191-280`
- `claude-code-source-code/src/utils/plugins/marketplaceManager.ts:154-191`

这些锚点共同指向同一事实：Claude Code 的“魔力”来自一个被显式组织的 runtime contract，而不是某个孤立模块。

## 1. 先说结论

继续深入后，可以把 Claude Code 的本体再压缩成五个平面：

1. 提示词平面：
   - 不是单段 system prompt，而是静态前缀、动态 section、上下文注入、附件提醒共同组成的合同。
2. 知识平面：
   - 不是“历史消息越多越好”，而是 `CLAUDE.md`、typed memory、session memory、relevant memories、plan/tasks 的分层持久化。
3. 协作平面：
   - 不是“多开几个 agent”，而是 coordinator 规则、task 对象、mailbox、team context、background runtime。
4. 生态平面：
   - 不是“支持插件”四个字，而是 marketplace、builtin plugin、bundled skill、MCPB、LSP、channels 各自不同的信任与治理模型。
5. 演化平面：
   - 不是“feature 多”，而是 build gate、runtime gate、compat shim、consumer subset 的多层发布制度。

这五个平面一起解释了：

- 为什么 prompt 会显得聪明
- 为什么长任务能持续
- 为什么多 Agent 不容易立刻失控
- 为什么生态面看起来丰富但又被明显分层
- 为什么它能快速演化却不必每次重写整套系统

## 2. 为什么提示词、知识层、权限和生态必须一起看

如果只看 prompt，会得到一个错误印象：

- 似乎强点只在文案

如果只看 tools，又会得到另一个错误印象：

- 似乎强点只在能力数量

如果只看 UI，则又会低估一件事：

- 长任务真正稳定运行，需要认知控制面和可恢复状态面一起成立

Claude Code 的源码说明，真正的闭环是：

1. `prompts.ts` 和 `systemPrompt.ts` 决定角色合同怎么装。
2. `context.ts`、`claudemd.ts`、`memdir.ts`、`SessionMemory` 决定哪些知识被稳定地带进上下文。
3. `attachments.ts` 决定哪些动态状态在 turn 间晚绑定注入。
4. `coordinatorMode.ts`、task framework、mailbox 决定多 Agent 如何分工与汇报。
5. `marketplaceManager.ts`、channels gate、LSP/MCPB 相关模块决定生态能力如何被治理。

所以“Claude Code 很强”的更准确说法不是：

- 它有一个很厉害的 agent

而是：

- 它把 agent 的提示词、知识、权限、协作和生态边界都做成了同一个 runtime 的不同平面

## 3. 对使用者的直接启发

### 3.1 不要把所有长期知识都塞进一层

更好的分法是：

- 项目长期规则：`CLAUDE.md`
- 私有项目习惯：`CLAUDE.local.md`
- 可复用长期记忆：typed memory / `MEMORY.md`
- 当前会话连续性：session memory
- 当前任务执行状态：plan / tasks

只要把这些层混成一层，Claude Code 的长处就会被你自己抵消掉。

### 3.2 多 Agent 时，先写自包含 prompt，再谈并行

源码已经把规则写死得很清楚：

- worker 看不到你的对话
- coordinator 不能把理解外包
- background task 和 task notification 是正式对象

因此，多 Agent 的最小正确用法不是：

- “你们几个自己看看”

而是：

- 明确任务范围
- 明确输出格式
- 明确 ownership
- 明确何时汇报

### 3.3 把生态支持和产品成熟度分开看

源码里出现：

- plugin
- marketplace
- MCPB / DXT
- LSP
- channels

不等于这些东西处在同一成熟度，也不等于都属于同一种信任层。

真正稳妥的做法是：

- 先分清“代码支持了什么 schema”
- 再分清“产品真正承诺了什么能力”

## 4. 对系统设计者的直接启发

### 4.1 Prompt 不是文案对象，而是契约对象

如果你想复制 Claude Code 的强项，优先应该复制的不是某个 system prompt，而是：

- 静态前缀与动态 section 的分离
- 角色 prompt 的分层
- 动态知识的附件化
- 缓存稳定性的结构约束

### 4.2 安全与 token 经济其实在做同一件事

两者都在限制无序扩张：

- 安全限制动作空间
- token 经济限制上下文空间

它们的共同目标都是：

- 让系统只暴露必要能力
- 让必要能力以更稳定、更可治理的形式暴露

### 4.3 生态的真正难点不是格式支持，而是信任与治理

支持 `.mcpb`、支持 `LSPTool`、支持 plugin marketplace，都不难写成“能跑”。

更难的是：

- 谁能默认启用
- 谁要走 allowlist
- 谁受 org policy 约束
- 哪些能力只允许某些 consumer subset 看见

Claude Code 的先进之处，正是在这里明显比普通 agent 产品更克制。

### 4.4 prompt 真正强在“行动语义更密”

如果继续沿 session memory、prompt suggestion、stop hooks 与 tool result replacement 往下追，会发现 Claude Code 不只是在做 prompt contract。

它还在做：

- 语义压缩

也就是说，它真正想保住的不是原始世界更多，而是：

- 未来回合仍然可继续工作的行动语义

### 4.5 runtime 其实在分配资源主权

如果继续沿 permissions、settings merge、managed hooks、token budget 与 context suggestions 往下追，会发现 Claude Code 也不只是“有安全层”和“有成本层”。

它更像在统一分配：

- 能力
- 时间
- 注意力
- 权威

这也是为什么模型在这里从来不是资源的主人，runtime 才是。

### 4.6 好源码的更深层价值是保留下一次重构可能性

如果继续沿 `query/config.ts`、`query/deps.ts`、leaf module 注释与 snapshot 逻辑往下追，会发现 Claude Code 的源码先进性也不只是“不变量治理”。

它还在持续保留：

- 下一次重排整套系统的可能性

这意味着它的工程高级感不只在当前分层，而在增长时仍不把未来重构的门焊死。

## 5. 蓝皮书接下来该怎么收束

这一轮之后，蓝皮书主线应继续坚持三条约束：

1. `bluebook/` 根目录只写一跳能懂的主线结论，不把字段级细节塞回主线。
2. 真正复杂的机制继续下沉到 `architecture/`、`api/`、`philosophy/`、`guides/`。
3. 每新增一个“看起来很强的能力”，都要追问三次：
   - 它的运行时契约是什么。
   - 它的知识与状态从哪里来。
   - 它的产品边界和治理边界在哪里。

## 6. 一句话总结

Claude Code 最难被抄走的，不是某一层，而是它把提示词、知识层、协作、权限和生态边界全部纳入了同一套 runtime contract。
