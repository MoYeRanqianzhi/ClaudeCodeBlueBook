# command-index nonmainline bucket cleanup 拆分记忆

## 本轮继续深入的核心判断

这一轮最值得收的，

不是继续大范围重分命令 taxonomy，

而是把 `01-命令索引.md` 里：

- `杂项与灰度入口`

这个混桶标题收成更准确的非主线表达。

## 为什么这轮值得单独提交

### 判断一：这一组里至少同时混着“非主线”与“条件公开”

现有边界证据已经够用：

- 能力状态矩阵把
  - `btw`、`stickers`、`think-back`
  这类写成：
  - 非主线功能
- Feature Gate 索引把
  - `ultraplan`
  写成：
  - 受 `ULTRAPLAN` 门控
- CLI 旗标索引又把
  - `--advisor`
  写成：
  - 条件公开但默认不应承诺的一组

所以这里的核心问题不是“这一组全都灰度”，而是：

- 这一组不是主线
- 其中有的还属于条件公开

## 这轮具体怎么拆

### 判断二：只改标题，并给最确定的两条补边界

这轮只做三件事：

1. 把分组标题从：
   - `杂项与灰度入口`
   改成：
   - `杂项与非主线入口`
2. 把 `/advisor` 标成：
   - 条件公开 / 高级控制面
3. 把 `/ultraplan` 标成：
   - 条件公开，受 `ULTRAPLAN` 门控

而：

- `/btw`
- `/stickers`
- `/think-back`
- `/thinkback-play`

仍只保留“非主线”语境，

不硬加没有现成证据的灰度标签。

## 为什么这轮不继续改整页其它分组

### 判断三：其它分组需要更细证据，这一组已经足够独立

当前最小有效切片是：

- 把“杂项与灰度”收成“杂项与非主线”

并补齐其中最明确的条件公开条目。

如果继续扩到更多分组，

就会变成另一轮更大的 taxonomy 重排。

## 主树状态记录

2026-04-08 在主仓库 `/home/mo/m/projects/cc/analysis` 执行：

- `git fetch origin main`
- `git pull --ff-only origin main`

结果是：

- `pull` 成功：`Already up to date.`

因此本轮继续深入仍只在：

- `.worktrees/userbook`

推进。

## 苏格拉底式自审

### 问：我是不是把这一组全都写成条件公开了？

答：没有。只有 `/advisor`、`/ultraplan` 有明确证据，其余仍保留在“非主线”语境里。

### 问：我是不是该把 `btw/stickers/think-back` 也强行写成灰度？

答：不该。当前证据只够支撑“非主线”，不够支撑统一灰度标签。

### 问：我是不是只换了标题，没有提升结构清晰度？

答：不是。这个标题决定读者先吸收成哪种桶，改成“非主线”后，至少不会再把整组误写成灰度面。
