# capability taxonomy top-level four-bucket sync 拆分记忆

## 本轮继续深入的核心判断

这一轮最值得收的，

不是重写能力边界索引，

而是把最上游总览层仍然残留的“三层桶”口径改回边界页已经建立好的四层口径。

## 为什么这轮值得单独提交

### 判断一：总览层不该先把“条件公开/灰度”和“影子面”压扁

当前边界正文已经明确：

- 稳定公开面
- 条件公开面
- ant-only / 内部面
- 只剩门控影子的缺失实现面

而且矩阵页也明确要防止：

- 稳定能力
- 条件能力
- 内部能力
- 影子能力

混写。

但根 `README` 和 `03-参考索引/README` 仍在说：

- 稳定 / 灰度 / 内部

这会让读者在真正进入能力边界页前，

先把：

- 条件公开
- 灰度
- 影子

压成一个模糊桶。

## 这轮具体怎么拆

### 判断二：只改最上游导览句，不扩到正文矩阵

这轮只改三处：

1. 根 `README` 的导航提示：
   - 从“稳定面、灰度面和内部面”
   - 改成“稳定面、条件/灰度面、内部面和影子面”
2. 根 `README` 的阅读原则：
   - 从“三层分层”
   - 改成“四桶分层”
3. `03-参考索引/README` 的问题列表：
   - 明确加入“条件公开或灰度”
   - 明确加入“只剩影子”

这样：

- 上游导览不再先压扁 taxonomy
- 但正文边界页和矩阵本身不需要重写

## 为什么这轮不直接拆“条件公开”和“灰度”在每一页的细分

### 判断三：这轮目标是恢复上游导览口径，不是重跑全书分类审计

条件公开和灰度在更细页面里是否需要继续分桶，

是下一层工作。

当前最小有效切片只是：

- 不让最上游继续写成三层

## 主树状态记录

2026-04-08 在主仓库 `/home/mo/m/projects/cc/analysis` 执行：

- `git fetch origin main`
- `git pull --ff-only origin main`

结果是：

- `pull` 成功：`Already up to date.`

因此本轮继续深入仍只在：

- `.worktrees/userbook`

推进。

## 苏格拉底式自审

### 问：我是不是把 taxonomy 做得更复杂了？

答：不是。复杂性早就在正文里存在，这轮只是让最上游导览别先把它抹平。

### 问：我是不是把“条件公开”和“灰度”硬拆成两列了？

答：没有。这轮仍保留“条件公开或灰度”这一上游合桶，只是把它和“影子面”分开。

### 问：我是不是该顺手改所有出现“灰度”的地方？

答：不需要。当前问题只在最上游总览层先把四层边界讲成三层。
