# schema-store-shell subfamily hub uplift 拆分记忆

## 本轮继续深入的核心判断

在当前 worktree 已经补完：

- `128-138` 的 subfamily hub
- `204` 的 stable-gray hardening

之后，

当前更值钱的下一刀，

不是继续回头补旧页正文，

而是把：

- `133-138`

从 `128-138` 里再抽成一个更窄的子家族入口。

## 为什么这轮值得单独提交

### 判断一：`133-138` 已经天然收成三条两步后继线

这一段现在已经清楚分成：

- `133-137` schema/store 到 cross-frontend consumer path
- `134-136` bridge chain 到 v2 active surface
- `135-138` foreground runtime 到 shared interaction shell

而且这三条线已经被：

- `215`
- `380`

稳定钉死。

这说明它已经不只是 `128-138` 中的一条长尾，

而是可以被单独浏览的一段更窄子家族。

### 判断二：这一刀比继续补正文更值，因为 `215` 已经有足够厚的正文分层

`215` 现在已经有：

- 页首范围声明
- stable / conditional / gray 分层

相比之下，

读者如果只关心 schema/store、bridge chain、interaction shell 的分裂，

仍需要先进入 `128-138` 再往下扫一遍。

所以更稳的最小改动不是再给 `215` 加一层注，

而是加一个 README-only subfamily hub，

把这三条两步后继线直接抽出来。

## 这轮具体怎么拆

这轮只做三件事：

1. 新增：
   - `133-138-schema-store、bridge chain 与 interaction shell 家族/README.md`
2. 在主 `README.md`
   里，
   给 `family hub` 再补一条入口
3. 新增这条 `397` 记忆，
   记录为什么这轮优先抽出 `133-138`

这样：

- `133-138` 会从“`128-138` 的后半段”
  变成“可直接跳入三条两步后继线”的更窄入口
- 目录结构仍保持 README-only 模式
- 后续如果还要细化到 `135-138`，也会有一条可复用模板

## 苏格拉底式自审

### 问：为什么这轮不是继续补 `215` 的 stable / conditional / gray？

答：因为 `215` 已经具备和相邻成熟结构页一致的三层分账。当前更缺的是目录层怎样让人直接跳到这一簇，而不是再补一段正文。

### 问：为什么 `133-138` 比 `122-127` 更适合先抽子家族？

答：因为 `122-127` 这条线在 `212/213` 里已经更像完整双主干，读者通常不会只看其中一半；而 `133-138` 的三条两步线更适合被单独浏览。

### 问：为什么仍然不搬原始记忆文件？

答：因为当前最需要优化的是导航，不是物理路径。先加 subfamily hub，后谈搬迁，风险更低。
