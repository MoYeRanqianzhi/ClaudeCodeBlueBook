# control-readme priority-heading cleanup 拆分记忆

## 本轮继续深入的核心判断

这一轮最值得收的，

不是重写 `05-控制面深挖/README.md` 的控制面清单，

而是把：

- `优先控制面`

这个标题

收回到不带控制面位阶暗示的表述。

## 为什么这轮值得单独提交

### 判断一：当前问题不在下面列了什么，而在标题把“常见起手入口”误写成了“控制面之间有优先级”

这页当前真实做的事情更像：

- 先给出最常见、最值得先落脚的控制面入口

而不是：

- 给控制面本身排等级

但：

- `优先控制面`

这个标题会把读者带向另一种理解：

- `01-19` 是更高一层
- 后面的控制面是次级层

这属于标题级的层级信号残留。

### 判断二：最小修正不是改列表顺序，而是改标题语气

当前问题不是：

- 列表顺序错了

而是：

- 标题的元话语太像位阶宣告

所以这轮最小切片不是挪列表，

而是把标题改成：

- `常见起手控制面`

这样：

- 仍保留“先给常见入口”的功能
- 但去掉“控制面本身有优先级”的暗示

## 这轮具体怎么拆

### 判断三：只改标题，不动控制面清单本体

这轮只做一件事：

1. 把：
   - `## 优先控制面`
   改成：
   - `## 常见起手控制面`

这样：

- 标题语气从“位阶排序”
  收成“起手入口”
- 下方条目一律保持不动
- 改动仍然限制在单文件单标题

## 为什么这轮不顺手改 `00-导读.md` 或根 `README`

### 判断四：当前最小有效改动只是修这一个标题的信号，不是做多层前门联动

这轮修的是：

- `05-控制面深挖/README.md`

自己的标题信号。

如果同时去动：

- `00-导读.md`
- 根 `README`

就会把：

- 单标题降位

扩大成：

- 多层入口联动改写

超出当前最小提交边界。

## 主树状态记录

2026-04-08 在主仓库 `/home/mo/m/projects/cc/analysis` 执行：

- `git fetch origin main`
- `git pull --ff-only origin main`

结果是：

- `pull` 成功：`Already up to date.`

因此本轮继续深入是在主树已同步的前提下，

仍只在：

- `.worktrees/userbook`

推进。

## 苏格拉底式自审

### 问：我是不是把“常见起手入口”误写成了“控制面本身有优先级”？

答：是。这正是这一刀要修的标题误导。

### 问：如果这页只是帮助读者先落脚，为什么我要让标题像是在给控制面排等级？

答：不该这样写。这里要强调的是起手价值，而不是位阶。

### 问：我是不是应该一并改掉这页所有类似句子？

答：不必。当前最小有效改动只是先把最显眼的标题收正。
