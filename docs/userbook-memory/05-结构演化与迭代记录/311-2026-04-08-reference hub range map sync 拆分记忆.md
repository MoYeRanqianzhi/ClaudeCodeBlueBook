# reference hub range map sync 拆分记忆

## 本轮继续深入的核心判断

这一轮最值得收的，

不是去重写根前门，

而是先把：

- `03-参考索引/README.md`

里已经写错的范围回跳同步回边界索引真相。

## 为什么这轮值得单独提交

### 判断一：这是 section hub 的导航真相错误，不是理解分歧

当前 `03-参考索引/README.md` 在 `201-204` 这组范围说明里，

把回跳范围写成了：

- `117-121`
- `122-128`
- `128-132`
- `133-138`

但边界索引和阅读路径已经明确对应范围是：

- `128-132`
- `133-138`
- `139-143`
- `144-149`

所以这不是风格问题，

而是 section hub 自己把导航范围写偏了。

## 这轮具体怎么拆

### 判断二：只改 4 条范围句，不扩到其它 hub

这轮只同步 `03-参考索引/README.md` 的 4 条说明：

1. `201` 对应 `128-132`
2. `202` 对应 `133-138`
3. `203` 对应 `139-143`
4. `204` 对应 `144-149`

保留后面的解释句不动，

只把前面的“你已经在什么范围里来回跳”改正确。

## 为什么这轮不一起处理根 README 的 front-door 深页直链

### 判断三：那是另一类结构职责问题，应该单独成刀

根 `README` 是否过深地直链 `04/05` 深页，

属于：

- 根前门与 section hub 的职责分配

而这轮是：

- section hub 自己的范围映射写错

两者不该混成一条 commit。

## 主树状态记录

2026-04-08 在主仓库 `/home/mo/m/projects/cc/analysis` 执行：

- `git fetch origin main`
- `git pull --ff-only origin main`

结果是：

- `pull` 成功：`Already up to date.`

因此本轮继续深入仍只在：

- `.worktrees/userbook`

推进。

## 苏格拉底式自审

### 问：我是不是在靠推测改索引编号？

答：没有。这轮直接以 `02-能力边界/README` 和 `00-阅读路径` 的已存在范围图为准。

### 问：我是不是该把 `201-204` 整组重新写成长解释？

答：不需要。当前错误只在前导范围句，最小有效切片就是把映射写对。

### 问：我是不是应该顺手修根 README 的深页直链？

答：不该。那是另一类 hub 职责问题，单独处理更稳。
