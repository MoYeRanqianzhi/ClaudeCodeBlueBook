# control continuity session-ops handoff cleanup 拆分记忆

## 本轮继续深入的核心判断

这一轮最值得收的，

不是再扩写 continuity 机制，

而是把控制面连续性页里残留的：

- 单点 `Export` 邻接口径

收回到已经成形的：

- 会话运营邻接层

并把失效的旧页跳转切回现有专题。

## 为什么这轮落点选 `05-控制面深挖/03-Compact、Resume、Memory：长任务连续性手册.md`

### 判断一：这页还停在旧的 `Rename/Export` 拆分前状态

当前其它层已经形成一致口径：

- continuity 本体是 `/memory`、`/compact`、`/resume`
- 当前工作对象怎样被命名、外化、分叉、回退和清空，应该 handoff 到会话运营层
- 对应长文专题已经是：
  - `04-专题深潜/07-会话运营、分叉与回退专题.md`

但控制面页这里仍把邻接层收窄成：

- `Export`

并继续跳到已经不存在的旧页：

- `08-Rename、Export：会话对象化与对外交付`

这会让控制面层重新把对象边界吸回旧版本。

## 这轮具体怎么拆

### 判断二：只把“邻近动作”从单点 `Export` 改成会话运营层，再修正 handoff

这轮只做两件事：

1. 把标题从：
   - `邻近但不属于连续性本体的动作：Export`
   改成：
   - `邻近但不属于连续性本体的动作：会话运营`
2. 把跳转从失效旧页切到：
   - `04-专题深潜/07-会话运营、分叉与回退专题.md`

正文不重写成命令表，

只把连续性页里的邻接对象重新提升到：

- 当前工作对象运营层

这样：

- continuity 三元组继续闭合
- session operations 邻接层重新对齐
- 旧页链接残留也一起收掉

## 为什么这轮不顺手做 front-door 术语清理

### 判断三：broken handoff + 旧对象边界比术语收尾更优先

并行只读审查还发现：

- 少量 `first-hop/second-hop` 元话语残留

但这些更偏前门术语收尾。

相比之下，这一页同时存在：

- 对象边界过窄
- 旧链接失效

因此更适合先单独提交。

## 主树状态记录

2026-04-08 在主仓库 `/home/mo/m/projects/cc/analysis` 执行：

- `git fetch origin main`
- `git pull --ff-only origin main`

结果是：

- `pull` 成功：`Already up to date.`

因此本轮继续深入仍只在：

- `.worktrees/userbook`

推进。

## 苏格拉底式自审

### 问：我是不是把“当前结果会被导出”误当成了“连续性邻接层就只剩 Export”？

答：不是。连续性旁边的其实是当前工作对象运营层，`Export` 只是其中一类外化动作。

### 问：我是不是因为已有 `07-会话运营、分叉与回退专题`，就不需要再动这页？

答：不对。只要控制面页还指向旧对象边界和旧页，读者就会在高层重新吸收一遍旧模型。

### 问：我是不是该把这一节扩写成完整 session operations 命令表？

答：不需要。完整命令分工已经在专题和索引里，这一页只需要把 continuity 与会话运营的 handoff 对齐。
