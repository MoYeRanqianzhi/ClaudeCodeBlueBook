# remote-truth skeleton hardening 拆分记忆

## 本轮继续深入的核心判断

在当前 worktree 已经把：

- `216` 的页首范围声明
- `139-149` 家族 README 的分流入口
- `215` 的 schema-store / bridge-shell skeleton

补齐之后，

当前更值钱的下一刀，

不是继续回头改 `139/140/141/142/143` 的 leaf 页，

也不是再扩新的 README / hub，

而是回到：

- `216-post_turn_summary visibility、mirror gray runtime、remote-session ledger 与 global remote bit：为什么 139、140、141、142、143 不是线性五连，而是接在三条后继线上的三组问题.md`

把这张结构页自己的尾部口径，

从半旧的：

- `稳定层与灰度层`

拉回到结构页已经更成熟的：

- `稳定阅读骨架 / 条件公开 / 内部证据层`

并补上 stop-line 与显式结论。

## 为什么这轮值得单独提交

### 判断一：当前缺口不在家族 handoff，而在 `216` 自己还没完全收成 skeleton 页

`139-149` 家族 README

现在已经把：

- `139-143`
- `144-149`

两簇的目录分流写清，

`381`

也已经把：

- `216` 不重讲 leaf proof

这件事记录进记忆。

所以当前真正落后的，

不是 handoff，

而是 `216` 自己还没有像 `204/215` 那样明确写出：

- 这里稳定的是三组后继问题的阅读骨架
- 不是把中间节点名升级成稳定公开能力

### 判断二：`139/140/141/142/143` 虽然分布在三条不同后继线上，但当前并没有哪一页明显掉队

这几页现在已经各自有：

- 窄主语
- 稳定 / 条件 / 灰度尾部
- 苏格拉底式自审

真正不统一的是：

- `216` 这种结构页

还没有把：

- stop-line
- `## 结论`
- 三组后继问题 vs 中间节点名

再切清一层。

所以这一刀更像：

- 结构页统一化

不是：

- leaf 页返工

### 判断三：这轮仍在优化目录结构，只是优化的是正文里的 truth 分叉骨架

`216`

本身就在承担：

- `215` 之后的下一跳分流

它不是普通综述页，

而是正文目录系统的一部分。

所以这轮继续优化“相关目录结构”，

更稳的做法不是再加入口，

而是把：

- 正文 truth 分叉骨架页自己的 stop-line 与结论

补硬。

## 这轮具体怎么拆

这轮只做三件事：

1. 把 `216-...三组问题.md`
   的尾部标题从：
   - `稳定层与灰度层`
   改成：
   - `稳定阅读骨架 / 条件公开 / 内部证据层`
2. 在尾部补一条 stop-line：
   - 明确 `139-143` 当前稳定的是三组后继问题
   - 不把 visibility、mirror、ledger、behavior bit 写成一条线性五连
3. 在苏格拉底式自审后补 `## 结论`
   - 明确 `140`
   - `139 -> 142`
   - `141 / 143`
   三组问题各自的根句
   - 以及它们分别接在哪条后继线上

## 苏格拉底式自审

### 问：为什么这轮不是继续回头补 `140` 或 `143`？

答：因为这些 leaf 页当前都已经成立。当前更明显的结构漂移，在 `216` 这种结构页还没完全切到统一收束口径。

### 问：为什么这轮不是继续扩 `139-149` 家族 README？

答：因为 README 现在已经把 `139-143 / 144-149` 的分流关系保护住了。当前收益更高的是把 `216` 自己的停笔边界写硬。

### 问：为什么这也算“优化相关目录结构”？

答：因为 `216` 就是正文目录系统里的局部骨架页。把它从“leaf 汇总语气”拉回“结构骨架语气”，本质上是在减少目录系统里的跨层串写。
