# settings index to self-check handoff 拆分记忆

## 本轮继续深入的核心判断

`settings/status/budget`

这条线现在已经有：

- 顶层 README 入口
- 路径 16
- `03-参考索引/01-命令工具/05`
- `05-控制面深挖/06`
- `05-控制面深挖/10`
- `04-专题深潜/10`

当前真正还缺的不是：

- 新路径
- 新专题
- 新顶层入口

而是：

- 索引页里从“对象分层”到“运行时自检三件套”的明确 handoff

## 为什么 `03/05` 需要补这句

### 判断一：这页既是对象分层入口，也是最容易让读者停住不动的地方

`03-参考索引/01-命令工具/05`

现在已经能回答：

- `/status` `/config` `/usage` 是 tab
- `/doctor` `/stats` 是独立 screen
- `/model` `/effort` `/fast` 是 session 调参

但用户在这里常常会分成两种真实问题：

1. 我只想弄清 `/status` `/doctor` `/usage` 这三件事怎么组成运行时自检
2. 我还想把 `/config` `/cost` `/stats` `/model` `/effort` `/statusline` 一并拆开

如果不把这两种问题分流，

读者就会在索引页里停住，

不知道下一步该去：

- `05-控制面深挖/06`
- 还是 `05-控制面深挖/10`

所以这轮补的是：

- question split handoff

不是：

- 新分析

## 为什么这轮优先送去 `05-控制面深挖/06`

### 判断二：`/status` `/doctor` `/usage` 三件套本来就是更窄的控制面对象

`05-控制面深挖/06`

专门回答：

- 宿主状态真相
- 环境可信度
- 运行时预算

这正好对应很多用户在索引页停下来的第一个问题：

- “我只想搞清这三件事”

相比之下，

`05-控制面深挖/10`

承担的是更宽的对象拆分：

- Settings tab
- 独立 screen
- session 调参
- 预算与限流分流

所以更稳的分流是：

- 只关心 triad，就去 `06`
- 还要做 wider object split，就留在 `05` 并继续去 `10`

## 为什么这轮不直接改 `05/06` 或 `05/10`

### 判断三：问题发生在索引页停顿，不发生在控制面正文

`05/06` 和 `05/10`

本身已经有各自清晰对象。

当前读者最容易卡住的点不是：

- 进了正文后不知道对象是什么

而是：

- 在索引页里不知道该往哪篇正文继续

所以这轮不该把改动扩大到正文页，

只需要在 `03/05` 先把 handoff 写明。

## 主树状态记录

2026-04-08 在主仓库 `/home/mo/m/projects/cc/analysis` 执行：

- `git pull --ff-only origin main`

实际返回：

- `Already up to date.`

随后本轮继续只在：

- `.worktrees/userbook`

推进 settings/status/budget 这条线的索引到控制面 handoff。

## 苏格拉底式自审

### 问：我现在缺的是再补一页，还是缺一句“只关心三件套就去 06”的分流？

答：如果页已经齐，缺的就是这句分流。

### 问：我是不是把“对象分层索引页”误当成终点，而不是下一跳分流器？

答：如果读者会停在这里不动，它就还没完成分流器职责。

### 问：我是不是又想去改 06/10 正文，而真正停顿发生在索引层？

答：如果问题发生在索引层，就先在索引层收口。
