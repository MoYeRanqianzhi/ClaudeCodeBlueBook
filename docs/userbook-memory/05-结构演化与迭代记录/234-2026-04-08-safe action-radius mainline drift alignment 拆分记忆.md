# safe action-radius mainline drift alignment 拆分记忆

## 本轮继续深入的核心判断

`安全放大工作面`

这条线的前门、阅读路径和顶层入口已经成立之后，

最容易残留的不是大结构缺口，

而是：

- 旧正文会把 `/add-dir` 轻轻拉回“上下文技巧”
- 或继续写成只在 worktree 旁边出现的邻近能力

所以这轮最稳的动作不是再补目录，

而是：

- 对齐三页主线/总览正文里的对象口径

## 为什么这轮先收旧正文回摆

### 判断一：结构已经修好，下一步要防止正文把读者拉回旧三分法

前几轮已经补了：

- 物理前门
- 稳定/灰度对齐
- 独立阅读路径
- 顶层入口到路径的接缝

如果正文仍然继续写成：

- `/add-dir` 只是上下文面的一种技巧
- 或“目录问题最终还是 worktree”

读者一回到主线页，

就会重新把：

- 扩工作面
- 隔离实现面

压成一件事。

所以现在最值钱的不是继续开新入口，

而是守住：

- object separation

## 为什么 `03-治理与安全专题` 要先补 `/add-dir`

### 判断二：这是前门之后最容易回摆的总览页

这页已经：

- 把读者先送去“安全放大工作面”
- 在稳定面里承认 `/add-dir`

但“正确入口”和“策略结论”仍主要围绕：

- permissions
- plan
- worktree

如果这里不补一段最短的 `/add-dir` 说明，

读者就会在总览页里重新得到一个旧结论：

- 目录相关问题最后都归 worktree

所以这轮把 `/add-dir` 拉回“正确入口”与策略顺序里。

## 为什么 `01-进入系统` 也值得顺手补一句

### 判断三：主线第一页负责建立工作目录对象感，不能默认只有 worktree 才算目录治理

这页已经写了：

- 工作目录不是静态背景
- 会话里可以切 worktree

但对用户最容易缺的一句其实是：

- `/add-dir` 是扩张当前工作面
- EnterWorktree 是隔离实现面

如果不把这句话说破，

“目录治理”在主线里就仍会显得：

- 主要靠 worktree 完成

所以这里适合补一句最短对比。

## 为什么 `01-提问、补上下文` 也要补分流提示

### 判断四：`/add-dir` 出现在上下文页，不等于它主要回答的是上下文问题

这页讨论的是：

- 材料怎么送进来
- 当前共同事实怎么校准

这里提到 `/add-dir` 没问题，

但如果不提醒：

- 当问题变成“要不要正式扩张工作面”时，应转去 safe action-radius 线

读者还是会把 `/add-dir` 误收进：

- 更会送上下文

所以这轮只补一个很短的分流提示，

不去重写整页对象。

## 为什么这轮仍然是低风险切片

### 判断五：只改桥接句和顺序结论，不重写目录和分桶

这轮没有改：

- 目录结构
- 稳定/条件/灰度分桶
- 新路径与新前门

只改：

- 总览页里的入口顺序
- 主线页里的对象对比句
- 上下文页里的分流提示

所以这是：

- drift alignment

不是：

- 新导航工程

## 主树状态记录

2026-04-08 在主仓库 `/home/mo/m/projects/cc/analysis` 执行：

- `git pull --ff-only origin main`

实际返回：

- `Already up to date.`

随后本轮只在：

- `.worktrees/userbook`

推进 safe action-radius 这条线的旧正文回摆对齐。

## 苏格拉底式自审

### 问：我现在缺的是新目录，还是缺几句把旧正文从错误对象上拉回来的桥接语？

答：如果目录层已经齐，而误判仍来自正文回摆，缺的就是桥接语。

### 问：我是不是因为 `/add-dir` 出现在上下文页，就默认它首先是上下文能力？

答：只要它正式改变工作面和 permission context，它首先回答的还是行动半径。

### 问：我是不是让主线页把目录问题继续默认写成 worktree 问题？

答：如果不把“扩张当前工作面”和“隔离实现面”直接对照，这个回摆就会一直在。
