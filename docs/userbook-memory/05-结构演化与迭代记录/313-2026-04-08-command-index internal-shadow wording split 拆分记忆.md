# command-index internal-shadow wording split 拆分记忆

## 本轮继续深入的核心判断

这一轮最值得收的，

不是再改命令工具区总图例，

而是把 `01-命令索引.md` 里具体条目层仍混在一起的：

- 条件公开
- 默认隐藏
- 内部命令
- 影子接近面

拆开。

## 为什么这轮值得单独提交

### 判断一：图例已澄清，但具体条目还会把旧桶重新讲回去

上一刀已经把命令工具区的图例说明校正成：

- 速读简写不等于正式 taxonomy

但 `01-命令索引.md` 这一页里，

仍有两处会把读者拉回旧模型：

1. `/remote-control` 仍写成：
   - `隐藏 / 条件公开`
   把展示属性和 rollout tier 并列写平
2. 底部分组仍叫：
   - `隐藏、内部或非普通用户面`
   继续把内部命令和影子接近面混成一桶

## 这轮具体怎么拆

### 判断二：只改代表性条目和分组标题，不扩到整页重分类

这轮只做三件事：

1. 把 `/remote-control` 改成：
   - `条件公开；默认帮助不显示`
2. 把分组标题从：
   - `隐藏、内部或非普通用户面`
   改成：
   - `内部命令与影子接近面`
3. 把 `teleport` 的说明改成：
   - slash 命令本体仍属内部 stub
   - 普通用户更可能接触到的是隐藏 CLI flag `--teleport` 这一影子接近面

这样：

- 条目层不会再把层级和展示属性揉成一个短语
- `teleport` 也不会再只落成含混的“内部 stub + 隐藏 CLI flag”

## 为什么这轮不把整页所有命令都按四层重写

### 判断三：这一页更大的 taxonomy 重构应另开一刀

当前高信号问题集中在：

- `/remote-control`
- 底部分组标题
- `teleport` 的影子说明

这些足够组成一个独立 commit。

再扩大到整页所有命令，

会把小切片变成大重排。

## 主树状态记录

2026-04-08 在主仓库 `/home/mo/m/projects/cc/analysis` 执行：

- `git fetch origin main`
- `git pull --ff-only origin main`

结果是：

- `pull` 成功：`Already up to date.`

因此本轮继续深入仍只在：

- `.worktrees/userbook`

推进。

## 苏格拉底式自审

### 问：我是不是把 `/remote-control` 写成完全不可见？

答：没有。它仍是条件公开，只是默认帮助不显示。

### 问：我是不是把 `teleport` 直接算成纯内部，不承认普通用户还能碰到它的公开接近面？

答：没有。这轮恰恰是把内部 stub 和隐藏 CLI flag 这两个层分开。

### 问：我是不是又在做词语美容？

答：不是。这里改的是命令对象的归属边界，而不是纯文风。
