# session plane copy inclusion cleanup 拆分记忆

## 本轮继续深入的核心判断

这一轮最值得收的，

不是再扩会话运营专题，

而是把功能面七分法里漏掉的：

- `/copy`

补回会话运营层。

## 为什么这轮落点选 `03-参考索引/04-功能面七分法.md`

### 判断一：高层总图一旦漏项，就会把对象边界重新讲窄

当前更具体的两层已经对齐：

- `04-专题深潜/07-会话运营、分叉与回退专题.md`
- `03-参考索引/06-高价值入口运行时合同速查.md`

都已经明确：

- `/copy`、`/rename`、`/export`、`/branch`、`/rewind`、`/clear`

同属：

- 当前工作对象运营层

但 `04-功能面七分法.md` 这里仍写成：

- `rename`、`export`、`branch`、`rewind`、`clear`

这会让高层功能面总图重新暗示：

- `/copy` 不属于会话运营层

## 这轮具体怎么拆

### 判断二：只补 `/copy`，并把总述句改成“保存并运营”

这轮只做两处最小修补：

1. 把总述句从：
   - `保存成`
   改成：
   - `保存并运营成`
2. 把会话运营动作列表补齐为：
   - `copy`、`rename`、`export`、`branch`、`rewind`、`clear`

这样：

- 高层总图与专题层重新一致
- `/copy` 不会再被漏写成层外杂项

## 为什么这轮不顺手改专题页里的细节 typo

### 判断三：对象边界漏项比局部排版错误更值得单独提交

同一轮复核里还能看到专题页里的局部小错误，

但这些不改变对象边界。

当前更高信号的是：

- 高层总图漏掉 `/copy`

所以这轮只补边界，不做顺手清扫。

## 主树状态记录

2026-04-08 在主仓库 `/home/mo/m/projects/cc/analysis` 执行：

- `git fetch origin main`
- `git pull --ff-only origin main`

结果是：

- `pull` 成功：`Already up to date.`

因此本轮继续深入仍只在：

- `.worktrees/userbook`

推进。

## 苏格拉底式自审

### 问：我是不是因为 `/copy` 没有改写 session id，就把它排除到会话运营层之外？

答：不能。`/copy` 解决的是当前工作对象怎样被带出终端，仍属于运营当前工作对象。

### 问：我是不是该把会话运营层重新写成长表？

答：不需要。当前问题只是高层总图漏掉 `/copy`，补齐即可。

### 问：我是不是在做纯术语补丁？

答：不是。漏掉 `/copy` 会直接改变高层对象边界，属于结构性遗漏。
