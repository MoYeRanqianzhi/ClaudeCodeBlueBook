# 139-143 remote truth split 拆分记忆

## 本轮继续深入的核心判断

`139-143` 还不能被写成：

- 一条从 mirror 一路走到 remote truth 的线性五连页

更准确的结构是：

- `140` = 接在 `133 -> 137` 后面的 visibility ladder zoom
- `139 -> 142` = 接在 `134 -> 136` 后面的 mirror/runtime pair
- `141 / 143` = 接在 `138 -> 204` 后面的 ledger truth 与 behavior-bit truth split

所以这轮最该补的是：

- 一张新的 `05` 局部结构页，把 `139-143` 收成三组后继问题
- 一张新的 `03` 索引页，把 `140`、`139/142`、`141/143` 的归属钉死
- 一条新的持久化记忆，记录为什么这轮不把 `139-143` 顺着编号继续往下抹平

## 为什么这轮不直接重写 `204`

### 判断一：`204` 已经足够回答 `138` 之后的大分叉

`204` 已经稳稳处理了：

- `132`
- `135`
- `138`
- `141`
- `142`
- `143`

之间的 trunk 关系。

这轮真正还没结构化收束的，不是 `204` 本身，

而是：

- `140` 其实不属于那条 trunk
- `139/142` 其实是一条 mirror gray-runtime 线

如果现在直接重写 `204`，

反而会把 `140` 这种 visibility 问题硬塞进 remote-truth 语境。

## 为什么这轮的三组最稳

### 判断二：`140` 是 `137` 的 zoom，不是 `141` 的前置页

`140` 回答的是：

- `post_turn_summary` 的 schema
- `@internal`
- stdout wire
- core SDK exclusion
- callback / terminal narrowing

所以它真正保护的是：

- visibility ladder

而不是：

- authoritative ledger

### 判断三：`139 -> 142` 是 mirror line，不是和 `141/143` 同层

`139` 先拆：

- startup intent
- implementation route
- runtime topology

`142` 再继续拆：

- hook 已 mirror
- core 仍可能 gray

所以这条线的主语一直是：

- mirror runtime topology

不是：

- remote truth split

### 判断四：`141` 与 `143` 更像远距对照 sibling

`141` 先保护：

- remote-session presence ledger

`143` 先保护：

- global remote behavior bit

它们都接在 `138/204` 之后，

但一个回答：

- ledger truth

一个回答：

- behavior truth

所以不能写成：

- `143` 只是 `141` 的布尔版

## 本轮最关键的新判断

### 判断五：mirror 线和 truth 线必须分开

这轮最值钱的保护句是：

- mirror 启动意图 / gray runtime 问题
- 不等于 remote truth 是哪一张账

如果这两者不分开，

就会把：

- `139`
- `142`
- `141`
- `143`

误写成同一条“remote 运行状态线”。

### 判断六：`post_turn_summary` 继续证明“可见性不是单层判断”

这轮还要继续记住：

- schema 存在
- `@internal`
- stdout wire 可承载
- core SDK 不接纳
- foreground 继续 narrowing

这些不是同一种可见性。

所以 `140` 也不该被拉去给 `141/143` 做前言。

### 判断七：本轮起笔前的主工作树状态已再次确认

本轮开始前已核对：

- `git fetch origin`
- `git rev-list --left-right --count HEAD...origin/main` = `0 0`
- `/home/mo/m/projects/cc/analysis` 上 `merge --ff-only origin/main` = `Already up to date.`
- 主树 `status` 为 `## main...origin/main`
- `userbook` worktree 起笔时为 `## userbook`

所以本轮继续只在：

- `.worktrees/userbook`

里推进，不触碰主树与其他 worktree。

## 苏格拉底式自审

### 问：我现在写的是可见性、mirror runtime，还是 remote truth？

答：如果一句话说不清自己属于哪组三页，就不要落字。

### 问：我是不是把 `140` 也拖进了 remote-truth 线？

答：先问它保护的是 visibility ladder，还是 authoritative ledger。

### 问：我是不是把 `142` 当成了另一张 truth 页？

答：如果一句话没有先经过 `139` 的 startup intent / implementation route / runtime topology，就还没真正进入 gray-runtime 问题。

### 问：我是不是把 `143` 当成 `141` 的缩略版？

答：先区分 presence ledger 与 global behavior bit；这两者不共享同一张 truth 表。
