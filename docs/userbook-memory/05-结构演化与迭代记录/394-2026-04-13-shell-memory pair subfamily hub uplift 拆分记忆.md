# shell-memory pair subfamily hub uplift 拆分记忆

## 本轮继续深入的核心判断

在当前 worktree 已经补完：

- `139-149` 的 subfamily hub

之后，

当前更值钱的下一刀，

不是继续回头补 `214/215`，

而是把：

- `144-149`

从 `139-149` 子家族里再抽成一个更窄的入口。

## 为什么这轮值得单独提交

### 判断一：`144-149` 已经天然收成三组稳定配对

这一段现在已经清楚分成：

- `144-145` pane / URL
- `146-147` contract thickness / command shell
- `148-149` headless env / remote memory

并且这些分组已经由：

- `217`
- `219`
- `382`

反复固定。

这说明它不再只是 `139-149` 中的一条长尾，

而是可以被单独浏览的一段更窄子家族。

### 判断二：这轮比继续补旧页 hardening 更值，因为目录层还可以直接缩短阅读路径

`214/215` 已经有 scope guard 和 stable / conditional / gray 分层。

相比之下，

`144-149` 读者如果只关心 pane / shell / memory，

现在仍要先进入 `139-149` 再下钻一次。

所以更稳的最小改动不是再补旧页，

而是加一个更细的 README-only subfamily hub，

把这三组配对直接抽成入口。

## 这轮具体怎么拆

这轮只做三件事：

1. 新增：
   - `144-149-session pane、command shell 与 remote memory 家族/README.md`
2. 在主 `README.md`
   里，
   给 `family hub` 再补一条入口
3. 新增这条 `394` 记忆，
   记录为什么这轮优先抽出 `144-149`

这样：

- `144-149` 会从“`139-149` 的后半段”
  变成“可直接跳入 pane / URL、command shell、headless memory 三组配对”的更窄入口
- 目录结构仍保持 README-only 模式
- 后续如果还要细化到 `148-149`，也有一条可复用模板

## 苏格拉底式自审

### 问：为什么这轮不是继续补 `214/215`？

答：因为这两页已经有页首页限和 stable / conditional / gray 分层。当前更缺的是目录层怎样让人直接跳到 `144-149`，而不是再给旧页加一层说明。

### 问：为什么 `144-149` 比 `128-138` 更适合先抽子家族？

答：因为 `144-149` 已经被 `217` 明确收成三组相邻配对，入口边界更干净；`128-138` 仍更依赖 `132` 作为更大的上游根页。

### 问：为什么仍然不搬原始记忆文件？

答：因为当前最需要优化的是导航，不是物理路径。先加 subfamily hub，后谈搬迁，风险更低。
