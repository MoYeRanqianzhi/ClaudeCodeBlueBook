# complete-nav second-hop clarification 拆分记忆

## 本轮继续深入的核心判断

最近几轮已经把：

- `02-能力地图`

从 root first-hop 并列入口降成了：

- second-hop 的机制总图层

但根页还有一处轻微泄漏：

- `完整结构导航`

仍会把：

- `02-能力地图`

和：

- `00`
- `01`
- `03`
- `04`
- `05`

并列列出来。

## 为什么这轮仍值得落一刀

### 判断一：这不是要再改路由，而是要显式区分“物理目录列举”和“阅读位阶”

前面已经在根页 first-hop 段落里收过：

- `02-能力地图` 不再作为并列前门

也已经在导读与阅读路径里收过：

- `02` 是 second-hop 的机制总图

所以这轮不再去改：

- first-hop 列表
- 阅读路径
- 导读

当前缺的是把：

- `完整结构导航`

明确标成：

- 物理目录结构总览

而不是让它在视觉上轻微回退成：

- first-hop 并列入口表

## 这轮具体怎么拆

### 判断二：加两句结构说明，比继续删目录项更稳

这轮只在根页 `完整结构导航` 前补两句：

- 这里只反映物理目录结构
- `02-能力地图` 出现在这里不等于回到 first-hop 位阶

这样：

- `02` 仍保留在完整目录总览里
- 但 second-hop 口径不会被目录列举重新冲掉

## 为什么这轮不顺手改其它 front-door 页面

### 判断三：其它页面已经对齐，这里是最后一个“结构列举像入口列举”的泄漏

并行只读复查说明：

- `README` / `00-*` / `03` / `04` README 的 first-hop 与 second-hop 基本已经对齐
- 最接近残留的就只剩这处结构导航

所以这轮只补：

- 根页 `完整结构导航`

的位阶说明。

## 主树状态记录

2026-04-08 在主仓库 `/home/mo/m/projects/cc/analysis` 执行：

- `git fetch origin main`
- `git pull --ff-only origin main`

结果是：

- `pull` 成功：`Already up to date.`

因此本轮继续深入仍只在：

- `.worktrees/userbook`

推进。

## 苏格拉底式自审

### 问：我是不是把“目录里存在”误当成了“阅读时应并列进入”？

答：物理结构存在不等于阅读位阶并列；完整导航需要说明这一点。

### 问：我是不是又想因为这里只是轻微泄漏，就完全不处理？

答：当主路由已收干净时，这类结构列举泄漏反而会成为最后的回退点，值得用最小说明封口。

### 问：我是不是该再删一次 `02-能力地图`？

答：不需要。它仍属于完整结构的一部分，当前问题只是缺少“结构总览不等于 first-hop”的说明。
