# command-index files visibility note 拆分记忆

## 本轮继续深入的核心判断

这一轮最值得收的，

不是重排 `01-命令索引.md` 的整个“上下文与连续性”分组，

而是给：

- `/files`

补上缺失的可见性边界。

## 为什么这轮值得单独提交

### 判断一：当前问题不在它属于哪个对象组，而在它被写成了裸稳定命令

`/files` 现在被放在：

- `上下文与连续性`

这一组里，

这本身未必错。

真正的问题是它当前文案只有：

- `列出当前上下文里的文件`

没有任何：

- `ant-only`
- `内部`

之类的边界信号。

这会让读者自然把它读成：

- 稳定公开命令

### 判断二：当前最小修正不是挪组，而是先补边界尾注

如果这轮直接把 `/files`

移去：

- `内部命令与影子接近面`

会牵动整页分组语义。

但当前最小有效改动只是：

- 先补 `ant-only / 内部`

这样：

- 读者立刻知道它不属于普通稳定面
- 这一刀仍保持在单句层面

## 这轮具体怎么拆

### 判断三：只在原句后补 `ant-only / 内部`

这轮只做一件事：

1. 把：
   - `/files`：列出当前上下文里的文件。
   改成：
   - `/files`：列出当前上下文里的文件，ant-only / 内部。

这样：

- `上下文与连续性` 组保留不动
- `/files` 的可见性边界被补齐
- 与同页其它已显式标边界的命令口径更一致

## 为什么这轮不顺手把 `/tag` `/files` 一起挪到内部区

### 判断四：那会把“补边界”扩大成“重排分组”

当前最小有效改动只是：

- 修一个明显漏标的内部命令

如果再把：

- `/tag`
- `/files`

一起挪组，

就会从：

- 单句边界修正

扩大成：

- 上下文与连续性组重排

超出当前最小提交边界。

## 主树状态记录

2026-04-08 在主仓库 `/home/mo/m/projects/cc/analysis` 执行：

- `git fetch origin main`
- `git pull --ff-only origin main`

结果是：

- `pull` 成功：`Already up to date.`

因此本轮继续深入是在主树已同步的前提下，

仍只在：

- `.worktrees/userbook`

推进。

## 苏格拉底式自审

### 问：我是不是把“能出现在索引里”误当成了“对普通用户稳定公开”？

答：是。这正是这一刀要修的误判。

### 问：如果 `/files` 真是普通稳定命令，为什么它还需要 ant-only 条件？

答：这说明它属于内部或特定用户面，而不是普适稳定面。

### 问：我是不是应该直接把它整个移出这组？

答：不必。当前最小有效修正只是先把可见性边界补清楚。
