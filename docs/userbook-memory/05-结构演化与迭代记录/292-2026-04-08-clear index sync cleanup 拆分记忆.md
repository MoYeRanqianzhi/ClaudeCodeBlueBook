# clear index sync cleanup 拆分记忆

## 本轮继续深入的核心判断

上一刀已经把：

- session operations 专题
- `路径 8`

里 `/clear` 的边界收回来了，

但索引层还残留旧语义。

这轮要做的，

不是再改专题正文，

而是把高频索引页同步到同一对象定义。

## 为什么这轮落点选三张索引页

### 判断一：专题层如果已改，索引层就不能继续把 `/clear` 写成“清空历史”

当前最显眼的残留有三处：

- `03-参考索引/05-任务到入口速查矩阵.md`
- `03-参考索引/01-命令工具/01-命令索引.md`
- `03-参考索引/06-高价值入口运行时合同速查.md`

它们分别还在暗示：

- `/clear` 属于“改写当前工作对象时间线”
- `/clear` 只是“清空当前历史”
- `/clear` 和 `/rewind` 没被显式拆开到“沿现有时间线动作” vs “切出新 frontstage”

如果不补这三处，

读者会继续先在速查层吸收旧主语，

从而抵消专题层的修正。

## 为什么这轮不再回头改更多正文

### 判断二：当前缺的是索引同步，不是再做第二轮专题扩写

`/clear`

的主语现在已经在专题层说清了：

- 不是 continuity
- 不是时间线回退
- 是新的空工作面 / 新 session id

所以这轮不需要：

- 再动 `07-会话运营、分叉与回退专题`
- 再动 `00-阅读路径`

只需要让索引层跟上。

## 这轮具体怎么同步

### 判断三：三个入口各改一句，就够把对象重新立住

- 任务矩阵：把“改写当前工作对象时间线”改成“改写当前时间线或切出新工作面”
- 命令索引：把“清空当前历史”改成“切出新的空工作面并生成新的 session id”
- 运行时合同速查：把 `/branch`、`/rewind` 与 `/clear` 明确写成两类动作

这样既不会扩写太多，

也能让：

- 矩阵
- 命令索引
- 合同页

三层口径重新一致。

## 主树状态记录

2026-04-08 在主仓库 `/home/mo/m/projects/cc/analysis` 执行：

- `git fetch origin main`
- `git pull --ff-only origin main`

结果是：

- `pull` 成功：`Already up to date.`

因此本轮继续深入仍只在：

- `.worktrees/userbook`

推进。

## 苏格拉底式自审

### 问：我是不是以为专题层改了，索引层就可以暂时错着？

答：如果读者第一跳先落在索引层，旧语义就还会覆盖专题修正。

### 问：我是不是把“清空当前历史”当成了足够接近 `/clear` 的简写？

答：不够。它会把 `/clear` 重新压回历史清理，而不是新工作面切换。

### 问：我是不是又想借这个同步机会顺手扩大到更多 session operations 文档？

答：这轮只做索引同步，避免把纠偏再次扩成大修。
