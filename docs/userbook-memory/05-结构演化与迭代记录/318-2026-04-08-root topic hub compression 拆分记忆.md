# root topic hub compression 拆分记忆

## 本轮继续深入的核心判断

这一轮最值得收的，

不是继续给根 `README` 的 `04-专题深潜` 段补主题清单，

而是把它从第二份“完整专题目录”压回一层摘要。

## 为什么这轮值得单独提交

### 判断一：根页这段已经在重复 `04-专题深潜/README.md` 的完整目录职责

当前根 `README` 的 `04-专题深潜` 段虽然没有直链到每个专题页，

但它仍逐条列出：

- 代码工作流
- 连续性与记忆
- 治理与安全
- 扩展与集成
- 并行执行与隔离
- 前端与远程
- 上下文接入与提示编译
- 评审、提交、导出与反馈
- 状态、额度、模型与节奏运营
- 终端交互与输入效率
- 会话发现与恢复选择
- 非交互、后台会话与自动化
- 初始化、安装与开工环境搭建
- 账户、隐私、资格与升级
- IDE、Desktop、Mobile 与会话接续
- 插件、MCP、技能、Hooks 与 Agents 运维
- CLI 根入口、旗标与启动模式
- 会外控制台与会内面板

而：

- `04-专题深潜/README.md`

已经承担：

- 前门切片
- 完整专题目录

所以根页继续把 18 个主题一条条列出来，

本质上还是在替代 section hub。

## 这轮具体怎么拆

### 判断二：保留主题面摘要，删掉 18 项清单，明确 handoff 到 `04-专题深潜/README.md`

这轮只做三步：

1. 把 18 条主题清单压成少量主题簇：
   - 代码与交付
   - 连续性、恢复与会话接续
   - 治理、安全与并行执行
   - 扩展、集成与工作环境
   - 前端、远端与自动化
   - 账户、模型、额度与日常运营
2. 保留这段“按真实工作目标组织”的一级摘要职责
3. 明确告诉读者：
   - 完整专题目录与前门切片请转去 `04-专题深潜/README.md`

这样：

- 根页仍告诉读者 `04` 这一层是什么
- 但完整目录 owner 重新回到 `04` section hub

## 为什么这轮不继续动 `05`

### 判断三：`05` 的 root deep ledger 已经在上一刀收掉，这轮只收 `04`

上一轮已经把根页 `05-控制面深挖` 的 leaf-level ledger 压回 section hub。

这一轮如果继续改，

最自然的对应切片就是：

- 收 `04`

不需要再回头动 `05`。

## 主树状态记录

2026-04-08 在主仓库 `/home/mo/m/projects/cc/analysis` 执行：

- `git fetch origin main`
- `git pull --ff-only origin main`

结果是：

- `pull` 失败：`Pulling is not possible because you have unmerged files.`

因此本轮继续深入仍只在：

- `.worktrees/userbook`

推进，不触碰主仓库冲突。

## 苏格拉底式自审

### 问：我是不是把根页收得太薄，读者会失去 `04` 的主题感？

答：没有。这轮保留了主题面摘要，只是不再逐条复制 section hub 的完整目录。

### 问：我是不是该把所有 18 条全删掉，直接只留一个链接？

答：不该。根页仍应告诉读者 `04` 这一层在处理什么，只是无需再展开成第二份目录。

### 问：我是不是又想顺手改 `04-专题深潜/README.md`？

答：不需要。当前 owner 页已经正确，问题只在根页越权重复目录。
