# settings usage product-plane alignment 拆分记忆

## 本轮继续深入的核心判断

`settings/status/budget`

这条线的顶层入口已经抬起来以后，

当前最容易残留的不是路由缺口，

而是：

- `/usage` 在不同页面里被写成“稳定主线”
- 却没有把它仍受 Claude AI 产品面和套餐语境约束这件事说稳

所以这轮更稳的动作不是继续补导航，

而是：

- 把专题 10 的结论分桶与主线 05、控制面 06 对齐

## 为什么这里要收 `/usage` 的口径

### 判断一：同一条线不能一边说“预算入口”，一边又把它写成对所有 provider 都同厚度存在

`01-主线使用/05`

已经明确提醒：

- `/usage` 依赖 Claude AI 订阅与对应产品面

`05-控制面深挖/06`

也已经明确提醒：

- 它不该被写成所有 provider 都同样存在的额度台

但 `04-专题深潜/10`

在“稳定主线”总结里原先把 `/usage` 直接写进稳定主线，

容易让读者得到一个过平的结论：

- 它和 `/status`、`/doctor` 一样，对所有登录/产品面都同厚度稳定

这不是这条线想表达的边界。

所以这轮只在结论分桶里补一句限定语。

## 为什么这轮不改正文主体

### 判断二：正文主体已经正确，回摆只发生在总结层

专题 10 正文里关于 `/usage` 的主体论证已经基本正确：

- 它是预算面
- 它不是账单尾巴
- 它影响长任务节奏运营

问题只出在页尾总结层：

- 稳定主线一栏写得过平

所以这轮更稳的是：

- 只修总结层

而不是：

- 回头重写整段 `/usage`
- 扩大到 `/cost`、`/stats`、`/extra-usage`

## 为什么这轮仍然值得单独提交

### 判断三：稳定/条件口径回摆会污染之后所有入口判断

前面几轮已经把：

- 顶层入口
- 路径层
- 索引层

逐步拉顺。

如果总结层继续回摆，

读者最终还是会在最短结论区得到：

- 一个过于平滑的“稳定主线”印象

所以即便这是小改动，

也值得单独收成一刀。

## 主树状态记录

2026-04-08 在主仓库 `/home/mo/m/projects/cc/analysis` 执行：

- `git pull --ff-only origin main`

实际返回：

- `Pulling is not possible because you have unmerged files.`

主仓库冲突仍未解决，

当前 `.worktrees/userbook` 仍保持干净，

所以本轮未触碰主仓库冲突，只继续在：

- `.worktrees/userbook`

推进 settings/status/budget 这条线的结论层口径对齐。

## 苏格拉底式自审

### 问：我现在缺的是新导航，还是缺把 `/usage` 的产品面约束重新写回总结层？

答：如果导航已齐，缺的就是总结层边界。

### 问：我是不是把“稳定可见”偷偷写成了“跨产品面同厚度存在”？

答：只要不补限定语，这个误读就会发生。

### 问：我是不是又想为了一个总结句去改整页正文？

答：如果主体论证已经对，扩大改动只会增加 churn。
