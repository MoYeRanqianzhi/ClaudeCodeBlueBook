# extension operations top-level lift 拆分记忆

## 本轮继续深入的核心判断

`/plugin`、`/mcp`、`/skills`、`/hooks`、`/agents`

这条扩展运维线现在已经有：

- 路径 91
- `04-专题深潜/17`
- `05-控制面深挖/02`

当前真正还缺的不是：

- 新前门
- 新路径
- 新专题

而是：

- 顶层 README 的独立目标入口

## 为什么这轮先抬顶层入口

### 判断一：这条线的高频误判发生在第一跳，而不是正文深处

用户最常见的误判不是：

- “我已经进了专题 17，但还看不懂”

而是：

- 我现在是在管插件包
- 管 MCP server
- 看 skills / hooks / agents 暴露面
- 还是让当前 session 吃到变更

而顶层 README 之前并没有把这条线当成一个独立目标抬出来。

这意味着用户虽然已经有：

- 路径 91
- 专题 17

但第一跳仍然要靠自己在目录里翻。

所以这轮该补的是：

- top-level discoverability

不是：

- 更深正文

## 为什么顶层入口应先落到路径 91

### 判断二：顶层入口回答“先去哪”，路径 91 回答“按什么顺序继续”

`路径 91`

已经把这条线收成：

1. 专题 17
2. 专题 04
3. 控制面 02

这条顺序本身就是：

- 从工作对象
- 到扩展总览
- 再到抽象层选择

的稳定阅读链。

所以顶层入口不该直接只落专题 17 或控制面 02，

而应先把读者交给：

- 路径 91

然后再在入口文案里补一句：

- 如果你主要卡在扩展层选择，再继续去控制面 02

## 为什么这轮不同时改专题层 README

### 判断三：先补最外层发现性，再决定是否需要中层接力

当前最明显的缺口在：

- `bluebook/userbook/README.md`

中层的 `04-专题深潜/README`

虽然也还没有显式抬这条线，

但它的收益低于顶层缺口，

因为用户连第一跳都还看不见。

所以这轮只补：

- 顶层 README

把扩展运维线先立起来。

## 主树状态记录

2026-04-08 在主仓库 `/home/mo/m/projects/cc/analysis` 执行：

- `git pull --ff-only origin main`

实际返回：

- `Already up to date.`

随后本轮继续只在：

- `.worktrees/userbook`

推进扩展运维线的顶层入口抬升。

## 苏格拉底式自审

### 问：我现在缺的是再补正文，还是缺让用户在顶层就知道这条线存在？

答：如果路径和专题都已有，缺的是顶层可发现性。

### 问：我是不是又想直接把用户扔进专题 17，而不是先给他一条稳定阅读顺序？

答：如果顶层入口直接落专题页，就跳过了路径层的排序价值。

### 问：我是不是为了少改一行，就继续容忍高频扩展对象在顶层没有独立入口？

答：如果它已经是高频线，就不该一直藏在目录里。
