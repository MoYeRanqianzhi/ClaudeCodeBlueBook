# cli heading nondefault-public cleanup 拆分记忆

## 本轮继续深入的核心判断

这一轮最值得收的，

不是继续改具体 flag 标签，

而是先把 `03-CLI 旗标与根命令索引.md` 里还在冒充 taxonomy 的分组标题收掉。

## 为什么这轮值得单独提交

### 判断一：标题本身在把“隐藏 / 灰度 / 内部”误写成并列桶

即便前一轮已经把：

- `条件/高级`
- `隐藏/灰度`
- `内部/托管`

明确说明成速读简写，

这页中段仍有两处标题继续把旧桶写回去：

- `隐藏、灰度或内部 flags`
- `条件、隐藏或宿主导向`

这些标题会让读者在尚未进入条目细节前，

先把：

- 展示属性
- rollout 条件
- 内部层

重新当成同一层 taxonomy。

## 这轮具体怎么拆

### 判断二：只改标题，不改具体 flag 标签

这轮只做两处标题收口：

1. 把：
   - `隐藏、灰度或内部 flags`
   改成：
   - `非默认公开的 flags`
2. 把：
   - `条件、隐藏或宿主导向`
   改成：
   - `宿主导向与非默认公开入口`

这样：

- 分组标题不再冒充正式 taxonomy
- 但条目层可以继续沿用现有速读标签，留待后续更细切片处理

## 为什么这轮不继续改 `remote-control/open` 等具体条目

### 判断三：条目层仍是下一层问题，不该和标题层混成同一刀

`remote-control`

以及其它条目里，

确实还残留更细的混写。

但这一轮的价值是：

- 先切断标题层的回流

而不是把整页所有条目一起重排。

## 主树状态记录

2026-04-08 在主仓库 `/home/mo/m/projects/cc/analysis` 执行：

- `git fetch origin main`
- `git pull --ff-only origin main`

结果是：

- `pull` 成功：`Already up to date.`

因此本轮继续深入仍只在：

- `.worktrees/userbook`

推进。

## 苏格拉底式自审

### 问：我是不是只是换了个更模糊的标题？

答：不是。`非默认公开` 恰好避免把不同层级和属性重新写成并列 taxonomy。

### 问：我是不是该一步到位把整页条目都改掉？

答：不需要。标题层和条目层应该拆开做，当前最小有效切片就是先收标题。

### 问：我是不是把宿主导向也当能力层级了？

答：没有。这轮正是把“宿主导向”放回使用语境说明，而不是正式能力桶。
