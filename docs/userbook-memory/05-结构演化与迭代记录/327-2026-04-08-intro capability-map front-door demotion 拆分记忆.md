# intro capability-map front-door demotion 拆分记忆

## 本轮继续深入的核心判断

这一轮最值得收的，

不是继续重写 `00-导读.md` 整个“先看哪里”区，

而是把：

- `能力地图`

这一条的入口位阶

再明确降一层。

## 为什么这轮值得单独提交

### 判断一：当前问题不在内容事实，而在前门位阶信号还没完全降下去

`00-导读.md` 当前已经在句尾补了：

- `更适合作为与 03-参考索引 配套使用的机制总图`

这其实已经承认：

- `02-能力地图`

不是和：

- `01-主线使用`
- `03-参考索引`
- `04-专题深潜`
- `05-控制面深挖`

同层的默认首跳入口。

但因为这句仍挂在：

- `如果你只想知道“先看哪里”`

下面，

读者第一眼仍会把它读成：

- 并列前门之一

所以这轮需要修的不是事实说明，

而是入口位阶信号。

### 判断二：最小修正不是删掉这条，而是显式写明“不是默认前门”

当前问题不是：

- `能力地图` 不该出现

而是：

- 它出现时还没有被明确降层

所以这轮最小切片不是删除它，

而是把它改成：

- `不是默认前门`

再接：

- `与 03-参考索引 配套使用的机制总图`

这样：

- 读者仍知道它存在
- 但不会再把它误读成默认第一跳

## 这轮具体怎么拆

### 判断三：只改这一条的位阶信号，不碰其它入口

这轮只做一件事：

1. 把 `能力地图` 这一条从：
   - `回答“系统在源码里究竟怎样组织这些能力”`
   改成：
   - `不是默认前门；更适合作为与 03-参考索引 配套使用的机制总图`

这样：

- 元话语从“并列入口”
  收成“配套总图”
- `00-导读` 的前门层级更一致
- 改动仍保持在单条 bullet

## 为什么这轮不顺手改根 `README` 里的机制总图入口

### 判断四：那是另一层前门，不该和 `00-导读` 的位阶提示混成一刀

根 `README` 里也有：

- `如果你要建立机制总图`

这一类入口。

但那是：

- 根前门

而当前这刀修的是：

- `00-导读` 的“先看哪里”位阶信号

如果一起改，

就会把：

- 导读层降位

扩大成：

- 多层前门联动重写

超出当前最小提交边界。

## 主树状态记录

2026-04-08 在主仓库 `/home/mo/m/projects/cc/analysis` 执行：

- `git fetch origin main`
- `git pull --ff-only origin main`

结果是：

- `pull` 成功：`Already up to date.`

因此本轮继续深入是在主树已同步的前提下，

仍只在：

- `.worktrees/userbook`

推进。

## 苏格拉底式自审

### 问：我是不是把“能从前门点进去”误写成了“应并列作为默认前门”？

答：是。这正是这轮要修的层级误判。

### 问：如果 `能力地图` 更适合作为机制总图，为什么我还把它挂在“先看哪里”的并列入口槽位里？

答：因为我混淆了“可达性”和“默认阅读顺序”。

### 问：我是不是应该直接把 `能力地图` 从这段里删掉？

答：不必。当前问题不是存在与否，而是位阶信号不够明确。
