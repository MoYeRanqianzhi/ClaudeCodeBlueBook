# capability taxonomy structured-output wording cleanup 拆分记忆

## 本轮继续深入的核心判断

这一轮最值得收的，

不是再回头重做 `StructuredOutput` 那个分桶提交，

而是补齐：

- `04-功能面七分法.md`

里残留的上位 taxonomy 旧口径。

## 为什么这轮值得单独提交

### 判断一：当前问题不在工具索引，而在上位 taxonomy 还保留旧的总括句

上一刀已经把：

- `02-工具索引`
- `02-能力地图/工具系统`

里的 `StructuredOutput`

从稳定工具桶里移出。

但 `04-功能面七分法.md` 这里仍写成：

- `基础文件、shell、Web、Notebook、LSP、结构化输出都在这一层`

这会让读者继续把：

- 结构化输出能力

和：

- `StructuredOutput` 这个工具本体

压成同一个稳定层级判断。

### 判断二：这页要修的是“总括句口径”，不是分桶结构

`04-功能面七分法.md`

本来就不是工具清单页，

它回答的是：

- 功能面怎么分层

所以这里不需要再造一个新的工具桶，

只需要把总括句改成：

- 常驻工具面是一类
- 结构化输出能力也在这一层，但它的工具本体更偏条件注入

这样：

- 上位 taxonomy 和下位工具索引重新一致
- 页面职责也不被拉歪

## 这轮具体怎么拆

### 判断三：保留“结构化输出也属于工具与资源池层”，撤掉“和常驻基础工具并列”的口气

这轮只做一件事：

1. 把原来的总括句拆成两条：
   - 常驻工具面：文件 / shell / Web / Notebook / LSP
   - 结构化输出：也经由这一层暴露，但更接近非交互 + `--json-schema` 路径里的条件注入

这样：

- taxonomy 仍然承认结构化输出属于这一层
- 但不再暗示 `StructuredOutput` 是默认常驻稳定工具

## 为什么这轮不顺手改 CLI / 自动化专题

### 判断四：那些页当前已经是支撑证据，不是冲突源

`03-CLI 旗标与根命令索引.md`

和

`13-非交互、后台会话与自动化专题.md`

目前都把结构化输出放在：

- `--json-schema`
- 非交互宿主

这条路径上。

它们没有继续把 `StructuredOutput` 写成稳定常驻工具。

所以这轮只补 taxonomy 页就够，

不必再把正确页卷进来。

## 主树状态记录

2026-04-08 在主仓库 `/home/mo/m/projects/cc/analysis` 执行：

- `git fetch origin main`
- `git pull --ff-only origin main`

结果是：

- `pull` 成功：`Already up to date.`

因此本轮继续深入是在主树已同步的前提下，

仍只在：

- `.worktrees/userbook`

推进。

## 苏格拉底式自审

### 问：我是不是把“属于这一层的能力”误写成了“以同样方式常驻暴露的工具”？

答：是。这就是 taxonomy 句子最容易产生的误导。

### 问：如果结构化输出能力仍属于工具与资源池层，为什么还要改这句？

答：因为要改的不是层级归属，而是“归属方式”的口气。

### 问：我是不是应该把“结构化输出”整个从这一层删掉？

答：不应该。能力归属没错，错的是把条件注入工具本体和常驻基础工具写成同一口径。
