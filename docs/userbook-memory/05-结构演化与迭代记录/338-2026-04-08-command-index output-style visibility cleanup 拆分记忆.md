# command-index output-style visibility cleanup 拆分记忆

## 本轮继续深入的核心判断

这一轮最值得收的，

不是重排整组“状态与运行时配置”命令，

而是把：

- `/output-style`

这条

从裸主线配置命令

收回到：

- 隐藏的过渡入口

这一口径。

## 为什么这轮值得单独提交

### 判断一：当前问题不在它属于配置面，而在它被写成了无边界的正常主线配置命令

`01-命令索引.md`

目前把：

- `/output-style`

和：

- `/config`
- `/model`
- `/fast`
- `/effort`

并列列在一起，

但它当前文案只有：

- `输出风格，已趋向由 /config 接管`

这还不足以告诉读者：

- 它其实是隐藏命令
- 而且已经是过渡入口 / 废弃入口

结果就容易被读成：

- 仍然属于稳定公开配置面

### 判断二：当前最小修正不是挪出分组，而是先把“隐藏 + 已废弃”补清楚

如果这轮直接把它从“状态与运行时配置”整组里移走，

就会牵动：

- 整组配置面结构

但当前最小有效改动只是：

- 在原句里补齐它的可见性和生命周期状态

这样：

- 读者立刻知道它不是普通主线配置命令
- `/config` 才是当前普通用户应走的正式入口

## 这轮具体怎么拆

### 判断三：只改这一句，不扩大到整组配置面重排

这轮只做一件事：

1. 把：
   - `/output-style`：输出风格，已趋向由 `/config` 接管。
   改成：
   - `/output-style`：隐藏的过渡入口，已废弃；普通用户改用 `/config`。

这样：

- 隐藏性被补出来
- 过渡 / 废弃状态被补出来
- 用户下一步该用 `/config` 也被说清

## 为什么这轮不顺手改 `/files` `/tag` 或其它漏标项

### 判断四：当前最小有效改动只是修一个明显仍留在主线配置组里的隐藏命令

前面已经逐步收过：

- `/files`
- `/desktop`
- `/status`

这轮再把 `/output-style`

补清楚，

刚好是另一个独立的“边界漏标”。

如果顺手再改其它条目，

就会把：

- 单行边界修正

扩大成：

- 命令索引一整组联动修订

超出当前最小提交边界。

## 主树状态记录

2026-04-08 在主仓库 `/home/mo/m/projects/cc/analysis` 执行：

- `git fetch origin main`
- `git pull --ff-only origin main`

结果是：

- `pull` 成功：`Already up to date.`

因此本轮继续深入是在主树已同步的前提下，

仍只在：

- `.worktrees/userbook`

推进。

## 苏格拉底式自审

### 问：我是不是把“仍列在索引里”误写成了“仍属于稳定公开配置面”？

答：是。这正是这一刀要修的误判。

### 问：如果普通用户应该走 `/config`，为什么我还让 `/output-style` 看起来像并列主线入口？

答：不该这样写。它更像隐藏的过渡口，而不是当前主线入口。

### 问：我是不是应该直接把它整个从这一组删掉？

答：不必。当前最小有效修正只是先把可见性与废弃状态补清楚。
