# root path-15 handoff link repair 拆分记忆

## 本轮继续深入的核心判断

这一轮最值得收的，

不是再做新的结构重排，

而是把根 `README` 里一处真实断句的路径 handoff 补回去。

## 为什么这轮值得单独提交

### 判断一：这是根前门导航句缺了一次明确回链

当前根 `README` 在“分清启动时决定什么，进会话后决定什么”这组里：

- 已经先给了 `00-阅读路径.md` 的“路径 14”

但下一句在转去：

- “路径 15：我想分清会外 root commands 和会内面板”

之前，

少了一次明确的：

- `00-阅读路径.md`

回链。

结果读起来就像：

- `再接着看`
- `里的“路径 15”`

中间缺了真正的承接对象。

## 这轮具体怎么拆

### 判断二：只补回链，不动该段其它导流结构

这轮只做一件事：

- 在根 `README` 这一句里把第二次 `00-阅读路径.md` 链接补回来

不改：

- 路径 14 / 15 的导流逻辑
- 后面的 section hub handoff
- 其它目标入口

这样：

- 根前门这组句子重新完整
- 但不会把这刀扩大成 root 导流策略重做

## 为什么这轮不顺手改其它“再接着看”句式

### 判断三：当前只确认到这一处是真断句，不该凭样式继续扩改

其它类似句式在当前根页里仍有明确目标页承接，

只有这一处是：

- 路径名在
- 回链不在

所以这轮只修这一处。

## 主树状态记录

2026-04-08 在主仓库 `/home/mo/m/projects/cc/analysis` 执行：

- `git fetch origin main`
- `git pull --ff-only origin main`

结果是：

- `pull` 成功：`Already up to date.`

因此本轮继续深入仍只在：

- `.worktrees/userbook`

推进。

## 苏格拉底式自审

### 问：我是不是只是在修排版，不是在修结构？

答：这不是纯排版；根前门在这里确实缺了一次实际导航 handoff。

### 问：我是不是该把路径 14 和路径 15 改成一条合并路径？

答：不需要。当前问题不是导流策略，而是第二条路径前的回链缺失。

### 问：我是不是应该顺手把整个根前门重新梳一遍？

答：不该。这一刀应保持最小，只补真实断句。
