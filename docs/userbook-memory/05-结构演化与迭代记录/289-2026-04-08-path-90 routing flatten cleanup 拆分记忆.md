# path-90 routing flatten cleanup 拆分记忆

## 本轮继续深入的核心判断

这轮更值得先修的，

不是多前端接续正文内容，

而是：

- `路径 90`
- `多前端接续与条件远端/README`

这两层入口在 front-door 上被压成了同一层。

## 为什么这轮落点选 `README.md` 与 `04-专题深潜/README.md`

### 判断一：阅读路径层和专题子前门层不该被写成同一跳

`00-阅读路径.md`

已经把：

- `路径 90：我想在 IDE、Desktop 和移动端之间继续同一工作`

定义成正式阅读路径，

而：

- `多前端接续与条件远端/README.md`

只是这条路径的第 1 跳。

但在：

- 根层 `README.md`
- `04-专题深潜/README.md`

里，

之前都直接把读者送进：

- `多前端接续与条件远端/README.md`

这会让：

- 阅读路径层
- 专题子前门层

被压成同一跳。

## 为什么这轮不改 `路径 90` 本体

### 判断二：当前缺的不是重写路径正文，而是让入口重新指回路径层

`路径 90`

本体已经清楚给出：

1. `多前端接续与条件远端/README`
2. `16-IDE、Desktop、Mobile 与会话接续专题`
3. `06-前端与远程专题`
4. `01-主线使用/01-进入系统`

顺序本身没有问题。

所以这轮不需要：

- 重写 `路径 90`
- 改多前端正文结构

只需要把前门口径改成：

- 先看 `路径 90`
- 再把子 README 说明成第一跳

就够了。

## 为什么这轮不顺手动别的远端入口

### 判断三：`/session`、`/remote-control` 和条件远端正文已经有自己的切片，当前缺口只在 route layering

前面已经收过：

- `/session` 与 `/remote-control` 的 front-door 比较块
- 多前端 / 条件远端相关的对象边界

这轮剩下的不是正文缺解释，

而是：

- root / topic hub 仍把子 front-door 写成阅读路径本体

所以这轮只改 routing，不扩正文。

## 主树状态记录

2026-04-08 在主仓库 `/home/mo/m/projects/cc/analysis` 执行：

- `git fetch origin main`
- `git pull --ff-only origin main`

结果是：

- `pull` 成功：`Already up to date.`

因此本轮继续深入仍只在：

- `.worktrees/userbook`

推进。

## 苏格拉底式自审

### 问：我是不是把“路径入口”与“路径里的第一跳”误当成了同一个对象？

答：如果 front-door 直接把读者送进子 README，而不先指向 `路径 90`，就是在压平层级。

### 问：我是不是又想通过重写多前端正文来解决其实只是 routing flatten 的问题？

答：当前更缺的是把入口指回阅读路径，而不是扩正文。

### 问：我是不是因为这是一个很小的导航修正，就想顺手再把别的远端入口一起捎上？

答：这会破坏单一提交边界；本轮只处理 `路径 90` 的 route layering。
