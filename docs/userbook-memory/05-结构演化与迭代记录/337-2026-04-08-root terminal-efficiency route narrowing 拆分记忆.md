# root terminal-efficiency route narrowing 拆分记忆

## 本轮继续深入的核心判断

这一轮最值得收的，

不是继续让根 `README` 只直达：

- `04-专题深潜/11-终端交互、状态栏与输入效率专题.md`

而是把：

- `提升终端交互效率`

这一条

收回到正式的：

- `路径 7`

上。

## 为什么这轮值得单独提交

### 判断一：当前问题不是缺入口，而是根前门又在重复维护一条更窄的交互效率路线

根 `README` 当前这条只把读者直接送到：

- `04-专题深潜/11-终端交互、状态栏与输入效率专题.md`

但 `00-阅读路径.md`

已经存在：

- `路径 7：我想让终端更顺手`

而且这条正式路径不只包含：

- `04-专题深潜/11...`

还继续把：

- `03-参考索引/01-命令工具/01-命令索引.md`

接在后面，

用来承接按键、Vim、状态栏、输入相关命令面的速查。

所以根前门这里的问题是：

- 只保留了主题专题这一半
- 但正式路径其实还把命令索引作为第二跳

### 判断二：直接点 04/11 容易让读者误以为“终端交互效率”只需要看专题页

当前写法最容易制造的误读是：

- 终端交互效率
  = 只看 `04-专题深潜/11...`

但正式的 `路径 7`

还需要：

- `01-命令索引.md`

来把具体入口收回来。

所以更稳的做法是：

- 根前门只保留对正式路径的 handoff

## 这轮具体怎么拆

### 判断三：把单页直达收成对 `路径 7` 的单点 handoff

这轮只做一件事：

1. 把根 `README` 里的：
   - `04-专题深潜/11-终端交互、状态栏与输入效率专题.md`
   收成：
   - `00-阅读路径.md` 里的“路径 7：我想让终端更顺手”

这样：

- 根前门不再维护更窄的终端交互局部路线
- 正式路径重新承担“专题页 + 命令索引”两跳编排
- 以后如果路径 7 调整顺序，根前门不用再同步局部节点

## 为什么这轮不顺手改“把结果交付给团队或外部流程”之外的其它目标

### 判断四：当前最小有效改动只是处理一条已存在正式路径的目标入口

前面几轮已经逐步把多条目标入口收回到阅读路径层。

这轮最小有效改动只是：

- 把“终端交互效率”这条也收回到路径 7

如果把其它剩余入口一起改，

就会把：

- 单目标入口去重

扩大成：

- 根前门成片重排

超出当前最小提交边界。

## 主树状态记录

2026-04-08 在主仓库 `/home/mo/m/projects/cc/analysis` 执行：

- `git fetch origin main`
- `git pull --ff-only origin main`

结果是：

- `pull` 成功：`Already up to date.`

因此本轮继续深入是在主树已同步的前提下，

仍只在：

- `.worktrees/userbook`

推进。

## 苏格拉底式自审

### 问：我是不是又把“正式路径里的第一跳专题页”误写成了“这个目标的全部入口”？

答：是。这正是这一刀要修的重复职责。

### 问：如果路径 7 已经存在，为什么根前门还只指向 04/11？

答：不该再只指向它。那会把正式路径压扁成更窄的局部版本。

### 问：我是不是应该把剩余所有目标入口都一次收回到路径页？

答：不应该。当前最小有效改动只是处理这条终端交互效率入口。
