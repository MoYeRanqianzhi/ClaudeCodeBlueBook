# reference-index task-entry wording cleanup 拆分记忆

## 本轮继续深入的核心判断

这一轮最值得收的，

不是继续重写 `03-参考索引/README.md` 整个“深问题清单”，

而是把：

- `面对真实任务时，应该先走哪个入口`

这句

收回到：

- `回查哪张入口速查页`

这一层。

## 为什么这轮值得单独提交

### 判断一：当前问题不在 `03` 没有入口价值，而在它被写回了“前门分流层”

`03-参考索引/README.md`

开头已经把自己定义成：

- 索引结构总览
- deep inventory / 回查定位

而且还明确说：

- 如果你已经带着具体误判或叶子问题回来回查，再继续看下面这份清单

在这样的定位下，

“深问题清单”里再出现：

- `面对真实任务时，应该先走哪个入口`

就会把 `03`

重新写回：

- 首入口分诊层

而不是：

- 回查 / 速查层

### 判断二：最小修正不是删掉这一项，而是把它收成“回查入口速查页”

当前问题不是：

- `03` 不该回答任务到入口的关系

而是：

- 它回答这件事时不该用“先走哪个入口”这种前门语言

因为 `03` 里确实有：

- `05-任务到入口速查矩阵.md`

所以最小有效改动不是删掉第 6 条，

而是把它改成：

- `面对真实任务时，应该回查哪张入口速查页`

这样：

- `03` 仍保留速查功能
- 但不再假装自己是默认首入口

## 这轮具体怎么拆

### 判断三：只改一句话，不碰其它问题清单

这轮只做一件事：

1. 把：
   - `面对真实任务时，应该先走哪个入口`
   改成：
   - `面对真实任务时，应该回查哪张入口速查页`

这样：

- 问题仍然保留
- 职责半径被收正
- `03` 与 `00-导读` / 根 `README` 的分工更清楚

## 为什么这轮不顺手改 `03-参考索引/README.md` 里的其它句子

### 判断四：当前最小有效改动只是收一处最明显的“前门化”残留

这页当然还可以继续精炼，

但当前最小、最独立、最不会牵动结构的一刀，

就是：

- 收掉一句“先走哪个入口”的前门语言

如果这轮再同时去改其它问题清单项，

就会把：

- 单句职责纠偏

扩大成：

- 整页 README 改写

超出当前最小提交边界。

## 主树状态记录

2026-04-08 在主仓库 `/home/mo/m/projects/cc/analysis` 执行：

- `git fetch origin main`
- `git pull --ff-only origin main`

结果是：

- `pull` 成功：`Already up to date.`

因此本轮继续深入是在主树已同步的前提下，

仍只在：

- `.worktrees/userbook`

推进。

## 苏格拉底式自审

### 问：我是不是把“速查某张入口页”误写成了“自己就是首入口分诊器”？

答：是。这正是这一刀要修的职责滑移。

### 问：如果 `03` 已经自称回查层，为什么我还让它说“先走哪个入口”？

答：不该这样写。那会让读者把 `03` 重新读成前门。

### 问：我是不是应该把任务到入口矩阵整个从 `03` 拿出去？

答：不必。当前问题不是矩阵不该存在，而是 README 对它的介绍口气太前门化。
