# intro source-evidence heading demotion 拆分记忆

## 本轮继续深入的核心判断

这一轮最值得收的，

不是重写 `00-导读.md` 下面整段源码锚点说明，

而是把：

- `源码证据怎么读`

这个标题

降回成一条可选支线提示。

## 为什么这轮值得单独提交

### 判断一：当前问题不在正文内容，而在标题把“回源码核对”抬成了导读默认职责

同一文件前面已经明确说：

- `bluebook/userbook/` 更偏“面向使用者的系统说明书”

但：

- `## 源码证据怎么读`

这个标题本身会给读者一个更强的默认信号：

- 读这本手册时，也应先把源码核对方式当主线阅读义务

这会让导读页的职责半径外扩。

### 判断二：最小修正不是删掉源码锚点，而是把它改成“按需回查”

当前问题不是：

- 这段源码锚点不该存在

而是：

- 它的标题语气太像默认主线

所以这轮最小切片不是删内容，

而是把标题收成：

- `如果你需要回源码核对`

这样：

- 读者仍能找到源码入口
- 但不会误读成导读的默认第一职责

## 这轮具体怎么拆

### 判断三：只改标题，不动下面的注册点与五个事实

这轮只做一件事：

1. 把：
   - `## 源码证据怎么读`
   改成：
   - `## 如果你需要回源码核对`

这样：

- 标题语气从“默认说明”
  收成“按需验证”
- 下方源码注册点与五个事实完全保留
- 改动仍然限制在单文件单标题

## 为什么这轮不顺手删掉整段源码锚点

### 判断四：源码锚点仍然有价值，当前只是不该占默认职责位

这段内容仍然帮助读者理解：

- 该从哪些注册点回源码核对

它的问题不是存在与否，

而是：

- 以什么位阶出现

如果整段删掉，

就会从：

- 层级信号修正

扩大成：

- 导读内容裁剪

超出当前最小切片边界。

## 主树状态记录

2026-04-08 在主仓库 `/home/mo/m/projects/cc/analysis` 执行：

- `git fetch origin main`
- `git pull --ff-only origin main`

结果是：

- `pull` 成功：`Already up to date.`

因此本轮继续深入是在主树已同步的前提下，

仍只在：

- `.worktrees/userbook`

推进。

## 苏格拉底式自审

### 问：我是不是把“给想核对源码的人一个回查入口”误写成了“所有读者都该先学源码入口”？

答：是。这正是这一刀要修的位阶误判。

### 问：如果这本手册本来就服务使用者，为什么我还让“回源码核对”用默认标题出现？

答：因为我没有把“主线阅读”与“证据回查”分成不同层级。

### 问：我是不是应该把这一段整个移到别的文件？

答：不必。当前最小有效改动只是降标题，不是搬内容。
