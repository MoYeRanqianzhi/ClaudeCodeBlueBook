# rename-export vs release-feedback split 拆分记忆

## 本轮继续深入的核心判断

这轮最值得先收的，

不是 `/export`、`/feedback`、`/release-notes` 再加更多正文，

而是把运行时合同速查页里仍然混在同一标题下的两组不同对象拆开。

## 为什么这轮落点选 `03-参考索引/06-高价值入口运行时合同速查.md`

### 判断一：`/rename`、`/export` 和 `/release-notes`、`/feedback` 不是同一类工作对象

`/rename`

与：

- `/export`

共同回答的是：

- 当前工作对象怎样被命名
- 当前工作对象怎样被正式外化

它们仍属于：

- 当前会话对象运营

而：

- `/release-notes`
- `/feedback`

回答的是：

- 产品变化怎样回到当前 CLI
- 使用摩擦怎样连同证据回流产品侧

它们更接近：

- 版本证据
- 产品反馈回路

这不是同一主语。

## 为什么这轮不把 `/tag` 也单独再拉出去

### 判断二：`/tag` 这里只是边界提醒，不构成本轮独立对象组

这页里：

- `/tag`

已经只剩一段：

- ant-only / 内部能力提醒

它不再承担稳定主线分工，

所以当前最小有效切片不是把 `/tag` 再单独抽标题，

而是先把：

- `/rename`、`/export`
- `/release-notes`、`/feedback`

两组正式对象拆开。

## 这轮具体怎么拆

### 判断三：只拆标题，不改正文合同

正文合同本身已经分别写清：

- `/rename`、`/export` 的工作对象运营语义
- `/release-notes`、`/feedback` 的版本 / 反馈语义

所以这轮不需要：

- 改写各小节内容
- 联动专题正文
- 重写误用段落

只需要把原来的总标题：

- `/rename`、`/export`、`/release-notes`、`/feedback`

拆成：

- `/rename` 与 `/export`
- `/release-notes` 与 `/feedback`

就够让读者在第一跳上重新得到正确主语。

## 为什么这轮不顺手动专题页

### 判断四：专题层已经按对象分好，混层只剩在速查页标题

`07-会话运营、分叉与回退专题`

已经把：

- `/rename`
- `/export`

放进当前工作对象运营。

`09-评审、提交、导出与反馈专题`

也已经把：

- `/export`
- `/release-notes`
- `/feedback`

放在“把内部工作翻译给外部流程与产品回路”的专题里。

这轮残留的问题只在：

- 速查页标题

因此不需要带动其它页面一起改。

## 主树状态记录

2026-04-08 在主仓库 `/home/mo/m/projects/cc/analysis` 执行：

- `git fetch origin main`
- `git pull --ff-only origin main`

结果是：

- `pull` 成功：`Already up to date.`

因此本轮继续深入仍只在：

- `.worktrees/userbook`

推进。

## 苏格拉底式自审

### 问：我是不是因为这些命令都“带出会话或带回信息”，就把它们压成了同一个标题？

答：带出当前工作对象与带回产品信息不是同一条对象流。

### 问：我是不是又想通过重写正文来掩盖其实只是标题混层的问题？

答：这轮最小有效切片只是标题拆分，正文本身并不缺。

### 问：我是不是该把 `/tag` 一起并进这次大拆？

答：不需要。`/tag` 这里只是边界提醒，不是本轮最强对象矛盾。
